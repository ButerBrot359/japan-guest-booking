# Этап 6.6: ОТП на входе, инклюзивные брони, редизайн навигации — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перенести ОТП с подтверждения брони на вход (код в Telegram), сделать день выезда занятым на уровне БД и календаря, переделать навигацию (десктоп без вкладок, мобила с сегмент-контролом) и закрыть мелкие UX-баги.

**Architecture:** Бэкенд: `POST /auth/login` перестаёт выдавать куку и выпускает OTP-челлендж `LOGIN` через существующий `OtpService` → outbox → Kafka → бот; новый `POST /auth/verify` проверяет код и ставит куку. Брони становятся мгновенными (`CONFIRMED` сразу), весь `PENDING_OTP`-флоу удаляется. Exclusion-констрейнт переводится на инклюзивный `daterange(check_in, check_out, '[]')`. Фронтенд: двухшаговый `LoginCard`, удаление `OtpModal`, инклюзивная подсветка, новые компоненты `Greeting`/`TabBar`/`ActiveBookingSection`.

**Tech Stack:** Spring Boot 4.0.7 (Java 21, Maven), PostgreSQL + Flyway, Kafka + Go bot-service, React 19 + TS + Vite + TanStack Query + Tailwind v4, vitest + MSW, Testcontainers.

**Spec:** `docs/specs/2026-08-21-stage-6.6-login-otp-ux-redesign-design.md`

## Global Constraints

- Ветка: `stage-6.5-ux-iteration` (продолжаем её; PR #10 уже открыт).
- Бэкенд-тесты: из `backend-api/` — `./mvnw test` (полный прогон) или `./mvnw test -Dtest=ИмяКласса`.
- Фронтенд: из `frontend/` — сначала `nvm use 22`, затем `npx vitest run`, `npm run lint`, `npx tsc -b`.
- Jackson в Boot 4 — это `tools.jackson.databind.ObjectMapper` (НЕ com.fasterxml).
- Все пользовательские тексты — на русском, в тоне существующих («ты» для гостя).
- Комментарии в коде — на русском, объясняют ограничение, а не «что делает строка».
- Коммит после каждой задачи; сообщения в стиле `feat:`/`fix:`/`docs:` на русском.

---

### Task 1: Бэкенд — `POST /auth/login` выпускает LOGIN-челлендж вместо куки

**Files:**
- Create: `backend-api/src/main/resources/db/migration/V7__otp_login_action.sql`
- Create: `backend-api/src/main/java/com/batowka/guestbooking/auth/LoginService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java`
- Test (rewrite): `backend-api/src/test/java/com/batowka/guestbooking/auth/GuestLoginTest.java`

**Interfaces:**
- Consumes: `OtpService.issue(UserAccount, String action, Map<String,Object> payload)` (Propagation.MANDATORY), `Phones.normalize(String) → Optional<String>`, `UserAccountRepository.findByPhoneAndDeletedAtIsNull(String)`, `TelegramNotLinkedException` (пакет `booking`, уже маплен на 409 `TELEGRAM_NOT_LINKED`).
- Produces: `LoginService.requestCode(String rawPhone): void` (бросает `InvalidPhoneException`/`UnknownPhoneException`/`TelegramNotLinkedException`); `POST /api/auth/login` → `202 Accepted` без Set-Cookie. Челлендж: `otp_challenges` строка с `action='LOGIN'`, `payload='{}'`; outbox-событие `OTP_CODE`.

- [ ] **Step 1: Миграция V7 — действие LOGIN в CHECK-констрейнте**

```sql
-- ОТП переезжает с подтверждения брони на вход (этап 6.6)
ALTER TABLE otp_challenges DROP CONSTRAINT otp_challenges_action_check;
ALTER TABLE otp_challenges ADD CONSTRAINT otp_challenges_action_check
    CHECK (action IN ('CREATE_BOOKING', 'RESCHEDULE', 'CANCEL',
                      'ADMIN_PASSWORD_RESET', 'LOGIN'));
```

Имя констрейнта проверено: inline-CHECK колонки `action` в V1 Postgres называет `otp_challenges_action_check` (тот же приём был в V6 с `bookings_status_check`).

- [ ] **Step 2: Переписать `GuestLoginTest` под новый контракт (failing)**

Заменить тесты `knownFriendGetsAuthCookie` (удалить) на новые; хелпер `givenFriend` расширить параметром `chatId`:

```java
private Long givenFriend(String phone, Long chatId) {
    UserAccount friend = new UserAccount();
    friend.setPhone(phone);
    friend.setName("Маша");
    friend.setRole(Role.FRIEND);
    friend.setTelegramChatId(chatId);
    return users.save(friend).getId();
}

@Autowired org.springframework.jdbc.core.JdbcTemplate jdbc;

@Test
void loginSendsCodeInsteadOfCookie() throws Exception {
    givenFriend("+81300000001", 565001L);

    MvcResult result = mvc.perform(post("/api/auth/login")
                    .contentType(APPLICATION_JSON)
                    .content("{\"phone\": \"+81 300-00-0001\"}"))
            .andExpect(status().isAccepted())
            .andReturn();

    // куки нет — вход завершается только на /auth/verify
    assertThat(result.getResponse().getHeader("Set-Cookie")).isNull();
    assertThat(jdbc.queryForObject(
            "select action from otp_challenges where status = 'PENDING'", String.class))
            .isEqualTo("LOGIN");
    assertThat(jdbc.queryForObject(
            "select count(*) from outbox where event_type = 'OTP_CODE'", Integer.class))
            .isEqualTo(1);
}

@Test
void loginWithoutTelegramGets409() throws Exception {
    givenFriend("+81300000002", null);

    mvc.perform(post("/api/auth/login")
                    .contentType(APPLICATION_JSON)
                    .content("{\"phone\": \"+81300000002\"}"))
            .andExpect(status().isConflict())
            .andExpect(jsonPath("$.code").value("TELEGRAM_NOT_LINKED"));
}

@Test
void repeatedLoginReplacesChallenge() throws Exception {
    givenFriend("+81300000003", 565003L);
    for (int i = 0; i < 2; i++) {
        mvc.perform(post("/api/auth/login")
                        .contentType(APPLICATION_JSON)
                        .content("{\"phone\": \"+81300000003\"}"))
                .andExpect(status().isAccepted());
    }
    // старый челлендж вытеснен, активен ровно один
    assertThat(jdbc.queryForObject(
            "select count(*) from otp_challenges where status = 'PENDING'", Integer.class))
            .isEqualTo(1);
}
```

Тесты `unknownPhoneGets401`, `adminPhoneCannotUsePasswordlessLogin`, `malformedPhoneGets400`, `blankBodyGets400`, `logoutClearsCookie`, `sixthLoginAttemptInARowGets429` остаются как есть — их контракт не меняется.

- [ ] **Step 3: Прогнать тест — убедиться, что падает**

Run: `./mvnw test -Dtest=GuestLoginTest`
Expected: FAIL — `loginSendsCodeInsteadOfCookie` получает 204 и куку вместо 202.

- [ ] **Step 4: Реализация — `LoginService.requestCode` + контроллер**

`LoginService.java` (новый):

```java
package com.batowka.guestbooking.auth;

import com.batowka.guestbooking.booking.TelegramNotLinkedException;
import com.batowka.guestbooking.otp.OtpService;
import com.batowka.guestbooking.user.Role;
import com.batowka.guestbooking.user.UserAccount;
import com.batowka.guestbooking.user.UserAccountRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.Map;

@Service
@RequiredArgsConstructor
public class LoginService {

    private final UserAccountRepository users;
    private final OtpService otp;

    /** Шаг 1 входа: код в Telegram. Куку не выдаёт. */
    @Transactional
    public void requestCode(String rawPhone) {
        UserAccount user = findFriend(rawPhone);
        // без Telegram код доставить некуда — фронт покажет инструкцию привязки
        if (user.getTelegramChatId() == null) {
            throw new TelegramNotLinkedException();
        }
        otp.issue(user, "LOGIN", Map.of());
    }

    UserAccount findFriend(String rawPhone) {
        String phone = Phones.normalize(rawPhone).orElseThrow(InvalidPhoneException::new);
        // Роль ADMIN сюда не пускаем: беспарольный вход не должен выдавать админский токен
        return users.findByPhoneAndDeletedAtIsNull(phone)
                .filter(u -> u.getRole() == Role.FRIEND)
                .orElseThrow(UnknownPhoneException::new);
    }
}
```

`AuthController.login` заменить (поле `LoginService loginService` добавить в конструктор через `@RequiredArgsConstructor`; поля `users`/`jwt` остаются — их использует `adminLogin`):

```java
@PostMapping("/login")
public ResponseEntity<Void> login(@Valid @RequestBody LoginRequest body, HttpServletRequest request) {
    rateLimiter.check(request.getRemoteAddr());
    loginService.requestCode(body.phone());
    return ResponseEntity.accepted().build();
}
```

`/api/auth/**` уже `permitAll()` в `SecurityConfig` — трогать не надо.

- [ ] **Step 5: Прогнать тест — зелёный**

Run: `./mvnw test -Dtest=GuestLoginTest`
Expected: PASS (все тесты класса).

- [ ] **Step 6: Commit**

```bash
git add backend-api/src/main/resources/db/migration/V7__otp_login_action.sql \
  backend-api/src/main/java/com/batowka/guestbooking/auth/LoginService.java \
  backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java \
  backend-api/src/test/java/com/batowka/guestbooking/auth/GuestLoginTest.java
git commit -m "feat: вход шлёт LOGIN-код в Telegram вместо мгновенной куки"
```

---

### Task 2: Бэкенд — `POST /auth/verify` проверяет код и ставит куку

**Files:**
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/otp/OtpService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/LoginService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java`
- Create Test: `backend-api/src/test/java/com/batowka/guestbooking/auth/VerifyLoginTest.java`

**Interfaces:**
- Consumes: `LoginService.findFriend(String)` из Task 1; `JwtService.issue(Long, Role)`; `AuthController.noContentWithCookie(String, Duration)`.
- Produces: `OtpService.verifyByAction(Long userId, String action, String code): ChallengeResult` (MANDATORY-транзакция; бросает `NoActiveCodeException`/`InvalidCodeException`/`CodeExpiredException`); `LoginService.verify(String rawPhone, String code): String` (возвращает JWT); `POST /api/auth/verify {phone, code}` → `204` + кука `auth` на 30 дней.

- [ ] **Step 1: Написать `VerifyLoginTest` (failing)**

Код из outbox достаём как JSON-поле payload (плейн-код хранится только там):

```java
package com.batowka.guestbooking.auth;

import com.batowka.guestbooking.AbstractIntegrationTest;
import com.batowka.guestbooking.user.Role;
import com.batowka.guestbooking.user.UserAccount;
import com.batowka.guestbooking.user.UserAccountRepository;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.MvcResult;
import tools.jackson.databind.ObjectMapper;

import static org.assertj.core.api.Assertions.assertThat;
import static org.springframework.http.MediaType.APPLICATION_JSON;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@AutoConfigureMockMvc
class VerifyLoginTest extends AbstractIntegrationTest {

    @Autowired MockMvc mvc;
    @Autowired UserAccountRepository users;
    @Autowired JdbcTemplate jdbc;
    @Autowired ObjectMapper objectMapper;

    private static final String PHONE = "+81300000010";

    private void givenFriendAndCodeSent() throws Exception {
        UserAccount friend = new UserAccount();
        friend.setPhone(PHONE);
        friend.setName("Маша");
        friend.setRole(Role.FRIEND);
        friend.setTelegramChatId(565010L);
        users.save(friend);
        mvc.perform(post("/api/auth/login").contentType(APPLICATION_JSON)
                        .content("{\"phone\": \"" + PHONE + "\"}"))
                .andExpect(status().isAccepted());
    }

    private String sentCode() {
        String payload = jdbc.queryForObject(
                "select payload::text from outbox where event_type = 'OTP_CODE' order by id desc limit 1",
                String.class);
        return objectMapper.readTree(payload).get("code").asString();
    }

    private String verifyBody(String code) {
        return "{\"phone\": \"" + PHONE + "\", \"code\": \"" + code + "\"}";
    }

    @Test
    void validCodeSetsCookie() throws Exception {
        givenFriendAndCodeSent();

        MvcResult result = mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON).content(verifyBody(sentCode())))
                .andExpect(status().isNoContent())
                .andReturn();

        assertThat(result.getResponse().getHeader("Set-Cookie"))
                .contains("auth=", "HttpOnly", "SameSite=Lax", "Path=/");
        assertThat(jdbc.queryForObject(
                "select status from otp_challenges order by id desc limit 1", String.class))
                .isEqualTo("USED");
    }

    @Test
    void wrongCodeGives400WithoutCookie() throws Exception {
        givenFriendAndCodeSent();

        MvcResult result = mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON).content(verifyBody("000000")))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.code").value("INVALID_CODE"))
                .andReturn();
        assertThat(result.getResponse().getHeader("Set-Cookie")).isNull();
    }

    @Test
    void thirdWrongAttemptBurnsChallenge() throws Exception {
        givenFriendAndCodeSent();
        String realCode = sentCode();

        for (int i = 0; i < 2; i++) {
            mvc.perform(post("/api/auth/verify")
                            .contentType(APPLICATION_JSON).content(verifyBody("000000")))
                    .andExpect(status().isBadRequest())
                    .andExpect(jsonPath("$.code").value("INVALID_CODE"));
        }
        mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON).content(verifyBody("000000")))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.code").value("CODE_EXPIRED"));
        // сгоревший челлендж не принимает даже настоящий код
        mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON).content(verifyBody(realCode)))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.code").value("NO_ACTIVE_CODE"));
    }

    @Test
    void verifyWithoutLoginGives400NoActiveCode() throws Exception {
        UserAccount friend = new UserAccount();
        friend.setPhone(PHONE);
        friend.setName("Маша");
        friend.setRole(Role.FRIEND);
        friend.setTelegramChatId(565010L);
        users.save(friend);

        mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON).content(verifyBody("123456")))
                .andExpect(status().isBadRequest())
                .andExpect(jsonPath("$.code").value("NO_ACTIVE_CODE"));
    }

    @Test
    void verifyUnknownPhoneGives401() throws Exception {
        mvc.perform(post("/api/auth/verify")
                        .contentType(APPLICATION_JSON)
                        .content("{\"phone\": \"+81599999999\", \"code\": \"123456\"}"))
                .andExpect(status().isUnauthorized())
                .andExpect(jsonPath("$.code").value("UNKNOWN_PHONE"));
    }
}
```

Замечание: 4 успешных вызова в одном тесте (`thirdWrongAttemptBurnsChallenge` = login + 4 verify = 5 запросов) укладываются в лимит rate-limiter'а (5/мин с одного IP); `AbstractIntegrationTest` сбрасывает limiter перед каждым тестом.

- [ ] **Step 2: Прогнать тест — убедиться, что падает**

Run: `./mvnw test -Dtest=VerifyLoginTest`
Expected: FAIL — 404 на `/api/auth/verify` (эндпоинта нет).

- [ ] **Step 3: Реализация**

`OtpService`: тело нынешнего `verify` (строки 81–104) выделить в приватный `verifyRow(Map<String,Object> row, String code): ChallengeResult` и добавить:

```java
/** Проверяет код активного челленджа гостя по типу действия (вход — action LOGIN). */
@Transactional(propagation = Propagation.MANDATORY)
public ChallengeResult verifyByAction(Long userId, String action, String code) {
    return verifyRow(findActiveByAction(userId, action), code);
}

private Map<String, Object> findActiveByAction(Long userId, String action) {
    try {
        return jdbc.queryForMap("""
                select id, action, payload::text as payload, code_hash,
                       expires_at, created_at
                from otp_challenges
                where user_id = ? and status = 'PENDING' and action = ?
                """, userId, action);
    } catch (EmptyResultDataAccessException e) {
        throw new NoActiveCodeException();
    }
}
```

Старый `verify(Long, long, String)` пока остаётся (его удалит Task 3) и делегирует: `return verifyRow(findActive(userId, bookingId), code);`.

`LoginService` добавить:

```java
/** Шаг 2 входа: проверка кода. Возвращает JWT для куки. */
@Transactional
public String verify(String rawPhone, String code) {
    UserAccount user = findFriend(rawPhone);
    otp.verifyByAction(user.getId(), "LOGIN", code);
    return jwt.issue(user.getId(), user.getRole());
}
```

(поле `private final JwtService jwt;` добавить в `LoginService`).

`AuthController` добавить:

```java
public record VerifyRequest(@NotBlank String phone, @NotBlank String code) {
}

@PostMapping("/verify")
public ResponseEntity<Void> verify(@Valid @RequestBody VerifyRequest body, HttpServletRequest request) {
    rateLimiter.check(request.getRemoteAddr());
    return noContentWithCookie(loginService.verify(body.phone(), body.code()), COOKIE_TTL);
}
```

- [ ] **Step 4: Прогнать тесты auth-пакета**

Run: `./mvnw test -Dtest='com.batowka.guestbooking.auth.*'`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend-api/src/main/java/com/batowka/guestbooking/otp/OtpService.java \
  backend-api/src/main/java/com/batowka/guestbooking/auth/LoginService.java \
  backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java \
  backend-api/src/test/java/com/batowka/guestbooking/auth/VerifyLoginTest.java
git commit -m "feat: POST /auth/verify — код из Telegram завершает вход кукой"
```

---

### Task 3: Бэкенд — брони без ОТП: мгновенный CONFIRMED, удаление pending-флоу

**Files:**
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/BookingService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/BookingController.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/otp/OtpService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/calendar/CalendarService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/common/GlobalExceptionHandler.java`
- Delete: `booking/PendingBookingCleaner.java`, `booking/OverlapsOwnBookingException.java`, `otp/ResendTooSoonException.java` (+ их тесты, см. Step 3)
- Tests: rewrite `booking/CreateBookingTest.java`, `booking/RescheduleCancelTest.java`; delete `booking/ConfirmBookingTest.java`, `booking/CancelPendingTest.java`, `booking/CleanerAndResendTest.java`

**Interfaces:**
- Consumes: `OtpService.verifyByAction` больше НЕ нужен здесь — брони не трогают ОТП вообще.
- Produces: `BookingService.create(Long, LocalDate, LocalDate, String): CreateResult` — вставляет `CONFIRMED`, старая CONFIRMED-бронь гостя отменяется в той же транзакции; `CreateResult(long bookingId)` — поле `willReplaceBooking` УДАЛЕНО (запись `WillReplace` удалить); `BookingService.reschedule(Long userId, long bookingId, LocalDate in, LocalDate out): void` и `BookingService.cancel(Long userId, long bookingId): void` — применяются сразу. `BookingService.ACTIVE == List.of(BookingStatus.CONFIRMED)`. HTTP-контракт: `POST /api/bookings` → `201 {"bookingId": N}`; `PATCH /api/bookings/{id}` и `DELETE /api/bookings/{id}` → 204 с мгновенным эффектом; эндпоинты `POST /{id}/confirm`, `POST /{id}/resend-code`, `DELETE /pending` удалены (404).

- [ ] **Step 1: Переписать `CreateBookingTest` (failing)**

Ключевые новые/изменённые тесты (хелперы `guest`/`auth`/`body` файла не трогать):

```java
@Test
void createConfirmsImmediately() throws Exception {
    Long id = guest("+81320000001", 777101L);

    mvc.perform(post("/api/bookings").cookie(auth(id))
                    .contentType(APPLICATION_JSON).content(body("2027-06-01", "2027-06-05")))
            .andExpect(status().isCreated())
            .andExpect(jsonPath("$.bookingId").isNumber());

    assertThat(jdbc.queryForObject(
            "select status from bookings order by id desc limit 1", String.class))
            .isEqualTo("CONFIRMED");
    // ОТП больше не выпускается, уведомление о брони уходит сразу
    assertThat(jdbc.queryForObject(
            "select count(*) from otp_challenges", Integer.class)).isZero();
    assertThat(jdbc.queryForObject(
            "select count(*) from outbox where event_type = 'BOOKING_CONFIRMED'", Integer.class))
            .isEqualTo(1);
}

@Test
void newBookingReplacesOldConfirmed() throws Exception {
    Long id = guest("+81320000004", 777104L);
    jdbc.update("""
            insert into bookings(user_id, check_in, check_out, status)
            values (?, '2027-08-01', '2027-08-05', 'CONFIRMED')
            """, id);

    mvc.perform(post("/api/bookings").cookie(auth(id))
                    .contentType(APPLICATION_JSON).content(body("2027-09-01", "2027-09-03")))
            .andExpect(status().isCreated());

    assertThat(jdbc.queryForObject(
            "select status from bookings where check_in = '2027-08-01'", String.class))
            .isEqualTo("CANCELLED");
    assertThat(jdbc.queryForObject(
            "select status from bookings where check_in = '2027-09-01'", String.class))
            .isEqualTo("CONFIRMED");
}
```

Тест `overlapWithOwnBookingHintsReschedule` УДАЛИТЬ: пересечение со своей бронью больше не ошибка — старая бронь отменяется и заменяется новой (проверяется тестом выше; даты могут даже совпадать). Тест `existingActiveBookingIsReportedAsWillReplace` УДАЛИТЬ (поля в ответе нет). `takenDatesGive409` остаётся без изменений.

- [ ] **Step 2: Переписать `RescheduleCancelTest` (failing)**

Сохранить хелперы файла; заменить тела тестов на мгновенную семантику:

```java
@Test
void rescheduleAppliesImmediately() throws Exception {
    // подготовка: гость + его CONFIRMED-бронь (хелперы файла)
    mvc.perform(patch("/api/bookings/" + bookingId).cookie(auth(id))
                    .contentType(APPLICATION_JSON)
                    .content("{\"checkIn\": \"2027-10-10\", \"checkOut\": \"2027-10-12\"}"))
            .andExpect(status().isNoContent());

    assertThat(jdbc.queryForObject(
            "select check_in::text from bookings where id = ?", String.class, bookingId))
            .isEqualTo("2027-10-10");
    assertThat(jdbc.queryForObject(
            "select count(*) from outbox where event_type = 'BOOKING_RESCHEDULED'", Integer.class))
            .isEqualTo(1);
}

@Test
void cancelAppliesImmediately() throws Exception {
    mvc.perform(delete("/api/bookings/" + bookingId).cookie(auth(id)))
            .andExpect(status().isNoContent());

    assertThat(jdbc.queryForObject(
            "select status from bookings where id = ?", String.class, bookingId))
            .isEqualTo("CANCELLED");
    assertThat(jdbc.queryForObject(
            "select count(*) from outbox where event_type = 'BOOKING_CANCELLED'", Integer.class))
            .isEqualTo(1);
}
```

Проверки «чужая бронь → 403», «не CONFIRMED → 409 BOOKING_EXPIRED», «занятые даты → 409 DATES_TAKEN» в файле сохранить, убрав из них шаг подтверждения кодом.

- [ ] **Step 3: Удалить мёртвые тесты и прогнать (failing)**

```bash
git rm backend-api/src/test/java/com/batowka/guestbooking/booking/ConfirmBookingTest.java \
  backend-api/src/test/java/com/batowka/guestbooking/booking/CancelPendingTest.java \
  backend-api/src/test/java/com/batowka/guestbooking/booking/CleanerAndResendTest.java
./mvnw test -Dtest='com.batowka.guestbooking.booking.*'
```

Expected: FAIL (компиляция/поведение старого сервиса).

- [ ] **Step 4: Реализация — `BookingService`**

`create` целиком заменить:

```java
@Transactional
public CreateResult create(Long userId, LocalDate checkIn, LocalDate checkOut, String comment) {
    // до отмены старой брони — иначе новая «заменит» уже состоявшуюся поездку
    completePastBooking(userId);
    UserAccount user = requireTelegramLinked(userId);
    validateDates(checkIn, checkOut);
    datesLock.acquire();
    // блокировки админа: exclusion constraint их не видит, проверяем кодом под замком
    if (!blockedPeriods.findOverlapping(checkIn, checkOut.minusDays(1)).isEmpty()) {
        throw new DatesTakenException();
    }
    // сначала отмена старой CONFIRMED: освобождает частичный уникальный индекс
    // «одна CONFIRMED на гостя» и даты в exclusion constraint для новой брони
    bookings.findFirstByUserIdAndStatusOrderByIdDesc(userId, BookingStatus.CONFIRMED)
            .ifPresent(old -> {
                jdbc.update("""
                        update bookings set status = 'CANCELLED', cancelled_by = 'GUEST'
                        where id = ? and status = 'CONFIRMED'
                        """, old.getId());
                notifyBookingEvent(user, "BOOKING_CANCELLED",
                        old.getCheckIn(), old.getCheckOut(), "GUEST");
            });
    Long bookingId;
    try {
        bookingId = jdbc.queryForObject("""
                insert into bookings(user_id, check_in, check_out, status, comment)
                values (?, ?, ?, 'CONFIRMED', ?) returning id
                """, Long.class, userId, checkIn, checkOut, comment);
    } catch (DataIntegrityViolationException e) {
        // чужая бронь заняла даты — откат отменит и отмену старой брони
        throw new DatesTakenException();
    }
    notifyBookingEvent(user, "BOOKING_CONFIRMED", checkIn, checkOut, "GUEST");
    return new CreateResult(bookingId);
}
```

Записи наверху класса: `CreateResult` становится `public record CreateResult(long bookingId) {}`; запись `WillReplace` удалить. `ACTIVE` → `List.of(BookingStatus.CONFIRMED)`.

`requestReschedule` переименовать в `reschedule` и слить с `applyReschedule`:

```java
@Transactional
public void reschedule(Long userId, long bookingId, LocalDate checkIn, LocalDate checkOut) {
    // гость со вчерашней вкладкой не должен переписать даты состоявшейся поездки
    completePastBooking(userId);
    UserAccount user = requireTelegramLinked(userId);
    requireOwnership(bookingId, userId);
    validateDates(checkIn, checkOut);
    datesLock.acquire();
    if (!blockedPeriods.findOverlapping(checkIn, checkOut.minusDays(1)).isEmpty()) {
        throw new DatesTakenException();
    }
    int updated;
    try {
        updated = jdbc.update("""
                update bookings set check_in = ?, check_out = ?
                where id = ? and status = 'CONFIRMED'
                """, checkIn, checkOut, bookingId);
    } catch (DataIntegrityViolationException e) {
        throw new DatesTakenException();
    }
    if (updated == 0) {
        throw new BookingExpiredException();
    }
    notifyBookingEvent(user, "BOOKING_RESCHEDULED", checkIn, checkOut, "GUEST");
}
```

`requestCancel` переименовать в `cancel` и слить с `applyCancel`:

```java
@Transactional
public void cancel(Long userId, long bookingId) {
    completePastBooking(userId);
    UserAccount user = requireTelegramLinked(userId);
    requireOwnership(bookingId, userId);
    Map<String, Object> dates = jdbc.queryForMap(
            "select check_in, check_out from bookings where id = ?", bookingId);
    int updated = jdbc.update("""
            update bookings set status = 'CANCELLED', cancelled_by = 'GUEST'
            where id = ? and status = 'CONFIRMED'
            """, bookingId);
    if (updated == 0) {
        throw new BookingExpiredException();
    }
    notifyBookingEvent(user, "BOOKING_CANCELLED",
            ((java.sql.Date) dates.get("check_in")).toLocalDate(),
            ((java.sql.Date) dates.get("check_out")).toLocalDate(), "GUEST");
}
```

УДАЛИТЬ из `BookingService`: `confirm`, `confirmCreate`, `applyReschedule`, `applyCancel`, `requireStatus`, `resendCode`, `requireNotCancelled`, `cancelPending`, поле `OtpService otp` (и импорт). `activeBooking` и `updateComment`: убрать ветки `PENDING_OTP` (`.or(...)` удалить; в `updateComment` условие статуса — `status = 'CONFIRMED'`).

- [ ] **Step 5: Реализация — контроллер, OtpService, календарь, хендлер, чистильщик**

- `BookingController`: удалить методы `confirm`, `cancelPending`, `resendCode` и запись `ConfirmRequest`; `reschedule`/`cancel` вызывают новые имена `bookingService.reschedule(...)` / `bookingService.cancel(...)`.
- `OtpService`: удалить `verify(Long, long, String)`, `resend`, `expireActive`, `findActive(Long, long)`. Остаются `issue`, `verifyByAction`, `verifyRow`, `findActiveByAction`, `expireInNewTx`.
- Удалить файлы: `PendingBookingCleaner.java`, `OverlapsOwnBookingException.java`, `ResendTooSoonException.java`.
- `GlobalExceptionHandler`: удалить хендлеры `overlapsOwnBooking` и `resendTooSoon` (+ импорты).
- `CalendarService`: `OCCUPYING_STATUSES` → `List.of(BookingStatus.CONFIRMED)`.
- Поиск хвостов: `grep -rn "PENDING_OTP\|OverlapsOwnBooking\|ResendTooSoon\|cancelPending\|resendCode" backend-api/src/main` — должно остаться пусто (кроме `BookingStatus`, где значение enum сохраняем для старых строк БД, и V1/V6-миграций, которые не трогаем).

- [ ] **Step 6: Починить оставшиеся тесты бэкенда**

Run: `./mvnw test`
Ожидаемые точки правки (по компиляции/падениям):
- `booking/MyBookingsTest`, `auth/MeControllerTest`: сценарии с `PENDING_OTP`-бронью заменить на `CONFIRMED` (активная бронь теперь только CONFIRMED); JSON-поле `status` в ответах — `"CONFIRMED"`.
- `booking/DatesRaceTest`: гонка двух create — оба вставляют CONFIRMED, проигравший получает 409 `DATES_TAKEN`; убрать шаги confirm.
- `booking/LazyCompletePastBookingTest`, `booking/UpdateCommentTest`: убрать упоминания PENDING_OTP-веток.
- `otp/*`-тесты: методы `resend`/`expireActive`/`verify(bookingId)` удалены — тесты на них удалить, тесты `issue`-механики (вытеснение, TTL) переписать через `verifyByAction`, если они дергали удалённые методы.
- `calendar/*`: `OCCUPYING_STATUSES` больше не включает PENDING_OTP — сценарии «pending-бронь красит календарь» заменить на CONFIRMED.
Expected: PASS всего прогона.

- [ ] **Step 7: Commit**

```bash
git add -A backend-api
git commit -m "feat: брони без ОТП — мгновенный CONFIRMED, pending-флоу удалён"
```

---

### Task 4: Бэкенд — инклюзивные диапазоны: день выезда занят

**Files:**
- Create: `backend-api/src/main/resources/db/migration/V8__inclusive_booking_range.sql`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/BookingRepository.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/BookingService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/AdminBookingService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/calendar/CalendarService.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/calendar/BlockedPeriodService.java`
- Tests: modify `booking/CreateBookingTest.java`, `calendar/CalendarServiceTest.java`

**Interfaces:**
- Produces: БД-инвариант «бронь занимает [check_in, check_out] включительно»; `BookingRepository.findOverlapping(from, to, statuses)` — пересечение с включительным диапазоном дней (`checkIn <= to && checkOut >= from`); `/api/calendar` красит день `check_out` как `BOOKED` (с тем же `mine`/`guestName`).

- [ ] **Step 1: Написать failing-тесты**

В `CreateBookingTest` добавить:

```java
@Test
void checkoutDayIsTakenForNextGuest() throws Exception {
    Long masha = guest("+81320000005", 777105L);
    Long petya = guest("+81320000006", 777106L);
    jdbc.update("""
            insert into bookings(user_id, check_in, check_out, status)
            values (?, '2027-07-01', '2027-07-05', 'CONFIRMED')
            """, masha);

    // заезд в день выезда предыдущего гостя запрещён (решение владельца, этап 6.6)
    mvc.perform(post("/api/bookings").cookie(auth(petya))
                    .contentType(APPLICATION_JSON).content(body("2027-07-05", "2027-07-08")))
            .andExpect(status().isConflict())
            .andExpect(jsonPath("$.code").value("DATES_TAKEN"));
}
```

В `CalendarServiceTest` добавить тест (подстроить под стиль файла — там прямые вызовы `calendarService.getCalendar`):

```java
@Test
void checkoutDayIsPaintedBooked() {
    // бронь 2027-07-01 → 2027-07-05: заняты все дни ВКЛЮЧАЯ 5-е
    // (подготовка гостя и брони — хелперами файла)
    List<CalendarDay> days = calendarService.getCalendar(
            LocalDate.parse("2027-07-01"), LocalDate.parse("2027-07-06"), null);
    assertThat(days.get(4).status()).isEqualTo(DayStatus.BOOKED); // 5-е июля
    assertThat(days.get(5).status()).isEqualTo(DayStatus.FREE);   // 6-е свободно
}
```

Run: `./mvnw test -Dtest='CreateBookingTest,CalendarServiceTest'`
Expected: FAIL — заезд в день выезда сейчас проходит (201), 5-е июля сейчас FREE.

- [ ] **Step 2: Миграция V8**

```sql
-- Дом должен «выдохнуть» день между гостями: бронь занимает [check_in, check_out]
-- ВКЛЮЧИТЕЛЬНО (решение владельца, этап 6.6). Заодно отменяем зависшие
-- PENDING_OTP-брони: флоу подтверждения кодом удалён, чистильщика больше нет.
UPDATE bookings SET status = 'CANCELLED', cancelled_by = 'GUEST'
WHERE status = 'PENDING_OTP';

ALTER TABLE bookings DROP CONSTRAINT no_overlapping_bookings;
ALTER TABLE bookings ADD CONSTRAINT no_overlapping_bookings EXCLUDE USING gist (
    daterange(check_in, check_out, '[]') WITH &&
) WHERE (status = 'CONFIRMED');
```

- [ ] **Step 3: Правка Java-слоя**

`BookingRepository.findOverlapping` — запрос и javadoc:

```java
/**
 * Брони, пересекающие диапазон дней [from, to] включительно.
 * Бронь занимает [checkIn, checkOut] ВКЛЮЧИТЕЛЬНО (V8): checkIn <= to и checkOut >= from.
 */
@Query("""
        select b from Booking b join fetch b.user
        where b.status in :statuses
          and b.checkIn <= :to
          and b.checkOut >= :from
        """)
```

Вызовы с поправкой на инклюзивность (день выезда теперь тоже занят, `minusDays(1)` больше не нужен):
- `BookingService.create`: `blockedPeriods.findOverlapping(checkIn, checkOut)` (убрать `.minusDays(1)`).
- `BookingService.reschedule`: то же самое.
- `AdminBookingService.reschedule` (строка ~60): то же самое.
- `BlockedPeriodService.create`: вызов `bookings.findOverlapping(startDate, endDate, ...)` не меняется, но комментарий над ним обновить: «бронь занимает [checkIn, checkOut] включительно (V8), блокировка — включительно: конфликт, если checkIn <= end && checkOut >= start».

`CalendarService` — красим и день выезда (строки 55–60):

```java
for (LocalDate d = start;
     !d.isAfter(b.getCheckOut()) && !d.isAfter(to);
     d = d.plusDays(1)) {
    days.put(d, new CalendarDay(d, DayStatus.BOOKED, name, mine));
}
```

- [ ] **Step 4: Прогнать все тесты бэкенда**

Run: `./mvnw test`
Expected: PASS. Если упали тесты, полагавшиеся на «стыковку» броней (выезд = чужой заезд) — это намеренная смена правила: поправить их данные, раздвинув брони на день.

- [ ] **Step 5: Commit**

```bash
git add -A backend-api
git commit -m "feat: день выезда занят — инклюзивный exclusion constraint и календарь (V8)"
```

---

### Task 5: Бот — текст «Код для входа»

**Files:**
- Modify: `bot-service/internal/kafka/consumer.go` (case "OTP_CODE", строки ~64–71)
- Modify: `bot-service/internal/kafka/consumer_test.go`

**Interfaces:**
- Consumes: `events.OtpCode` уже содержит поле `Action string` (`internal/events/events.go:32`).
- Produces: для `action == "LOGIN"` бот шлёт «Код для входа: X. Действует 5 минут.», иначе прежний текст.

- [ ] **Step 1: Failing-тест**

В `consumer_test.go` рядом с существующим OTP-тестом (строка ~103, скопировать его структуру: `eventJSON`, фейковый sender):

```go
func TestOtpCodeLoginText(t *testing.T) {
	// action LOGIN → текст про вход, а не «подтверждение»
	// (собрать consumer и sender так же, как в соседнем OTP-тесте файла)
	_ = c.handle(context.Background(), eventJSON("e-otp-login", "OTP_CODE",
		`{"chat_id": 42, "code": "123456", "action": "LOGIN", "expires_at": "2026-01-01T00:00:00Z"}`))
	if got := sender.lastText; !strings.Contains(got, "Код для входа: 123456") {
		t.Fatalf("ожидали текст про вход, получили: %q", got)
	}
}
```

Run: `cd bot-service && go test ./...`
Expected: FAIL — текст «Код подтверждения».

- [ ] **Step 2: Реализация**

```go
case "OTP_CODE":
	var p events.OtpCode
	if err := json.Unmarshal(env.Payload, &p); err != nil {
		log.Printf("битый payload OTP_CODE: %v", err)
		return nil
	}
	text := "Код подтверждения: " + p.Code + ". Действует 5 минут."
	if p.Action == "LOGIN" {
		text = "Код для входа: " + p.Code + ". Действует 5 минут."
	}
	return c.send(ctx, env.EventID, p.ChatID, text)
```

- [ ] **Step 3: Прогнать и закоммитить**

Run: `cd bot-service && go test ./...` → PASS.

```bash
git add bot-service
git commit -m "feat: бот различает код для входа и код подтверждения"
```

---

### Task 6: Фронтенд — API-слой и двухшаговый вход

**Files:**
- Modify: `frontend/src/api/queries.ts`, `frontend/src/api/types.ts`, `frontend/src/test/handlers.ts`
- Rewrite: `frontend/src/components/LoginCard.tsx`, `frontend/src/components/LoginCard.test.tsx`
- Modify: `frontend/src/components/TelegramHint.tsx`, `frontend/src/components/BookingSheet.tsx`
- Modify: `frontend/src/pages/CalendarPage.tsx` (закрытие модалки, флоу без otp)
- Delete: `frontend/src/components/OtpModal.tsx`, `frontend/src/components/OtpModal.test.tsx`

**Interfaces:**
- Consumes: `POST /api/auth/login {phone}` → 202 | 401 `UNKNOWN_PHONE` | 409 `TELEGRAM_NOT_LINKED` | 429; `POST /api/auth/verify {phone, code}` → 204+кука | 400 `INVALID_CODE`/`CODE_EXPIRED`/`NO_ACTIVE_CODE` | 401.
- Produces: `useLogin(): мутация phone → void (202)`; `useVerifyLogin(): мутация {phone, code} → void, onSuccess инвалидирует ['me']`; `useRescheduleBooking`/`useCancelBooking` с onSuccess-инвалидацией `['me']`, `['calendar']`, `['my-bookings']`; тип `BookingStatus = 'CONFIRMED' | 'CANCELLED' | 'COMPLETED'`; `CreateResult { bookingId: number }`; `useMyBookings(enabled?: boolean)`.

- [ ] **Step 1: API-слой и моки**

`types.ts`: `BookingStatus` → `'CONFIRMED' | 'CANCELLED' | 'COMPLETED'`; `CreateResult` → `{ bookingId: number }`; интерфейс `WillReplace` удалить.

`queries.ts`:

```ts
export function useLogin() {
  // шаг 1: только отправляет код в Telegram, куки ещё нет
  return useMutation({ mutationFn: (phone: string) => api.post<void>('/auth/login', { phone }) })
}

export function useVerifyLogin() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (body: { phone: string; code: string }) => api.post<void>('/auth/verify', body),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['me'] }),
  })
}
```

Удалить `useConfirmBooking`, `useResendCode`, `useCancelPending`. В `useRescheduleBooking` и `useCancelBooking` добавить:

```ts
onSuccess: () => {
  qc.invalidateQueries({ queryKey: ['me'] })
  qc.invalidateQueries({ queryKey: ['calendar'] })
  qc.invalidateQueries({ queryKey: ['my-bookings'] })
},
```

(с `const qc = useQueryClient()` в обоих). `useMyBookings` не меняется — компоненты, которые его зовут, рендерятся только у залогиненных.

`handlers.ts`: добавить в `MockState` поле `pendingLoginPhone: string | null` (сброс в `resetMockState`); заменить обработчики auth и убрать confirm/resend/pending:

```ts
http.post('/api/auth/login', async ({ request }) => {
  const { phone } = (await request.json()) as { phone: string }
  mockState.pendingLoginPhone = phone
  return new HttpResponse(null, { status: 202 })
}),
http.post('/api/auth/verify', async ({ request }) => {
  const { phone, code } = (await request.json()) as { phone: string; code: string }
  if (phone !== mockState.pendingLoginPhone || code !== '123456') {
    return HttpResponse.json({ code: 'INVALID_CODE', message: 'Неверный код' }, { status: 400 })
  }
  mockState.me = {
    phone, name: 'Маша', role: 'FRIEND', telegramLinked: true,
    greeting: null, activeBooking: null,
  }
  return new HttpResponse(null, { status: 204 })
}),
```

`POST /api/bookings` в моках: `status: 'CONFIRMED'`, ответ `{ bookingId: 100 }`. Удалить хендлеры `/confirm`, `/resend-code`, `DELETE /api/bookings/pending`.

- [ ] **Step 2: Переписать `LoginCard.test.tsx` (failing)**

Ключевые сценарии (в стиле текущего файла — testing-library + msw):

```tsx
it('после отправки номера показывает шаг кода и входит по верному коду', async () => {
  render(<LoginCard />, { wrapper })
  await typePhone('7787886432') // хелпер файла: ввод в data-testid="phone-input"
  await userEvent.click(screen.getByRole('button', { name: 'Получить код' }))
  // шаг 2: поле кода
  const codeInput = await screen.findByLabelText('Код из Telegram')
  await userEvent.type(codeInput, '123456')
  await userEvent.click(screen.getByRole('button', { name: 'Войти' }))
  await waitFor(() => expect(mockState.me).not.toBeNull())
})

it('неверный код показывает ошибку и не пускает', async () => { /* код 000000 → текст «Неверный код» */ })

it('TELEGRAM_NOT_LINKED показывает инструкцию про бота', async () => {
  server.use(http.post('/api/auth/login', () =>
    HttpResponse.json({ code: 'TELEGRAM_NOT_LINKED', message: '' }, { status: 409 })))
  // после сабмита номера — текст «Мы шлём код входа в Telegram»
})

it('«Изменить номер» возвращает на шаг телефона', async () => { /* кнопка возврата */ })
```

Run: `npx vitest run src/components/LoginCard.test.tsx` → FAIL.

- [ ] **Step 3: Переписать `LoginCard.tsx`**

Полная замена (маску телефона и форму заявки на доступ сохранить как есть):

```tsx
import { useEffect, useLayoutEffect, useRef, useState } from 'react'
import { ApiError } from '../api/client'
import { useAccessRequest, useLogin, useVerifyLogin } from '../api/queries'
import { caretAfterDigits, formatPhone, phoneDigits, toApiPhone } from '../lib/phone'

const VERIFY_ERROR_TEXTS: Record<string, string> = {
  INVALID_CODE: 'Неверный код',
  CODE_EXPIRED: 'Код сгорел — отправь новый',
  NO_ACTIVE_CODE: 'Кода нет — отправь новый',
  RATE_LIMITED: 'Слишком часто — подожди минуту.',
}

export function LoginCard() {
  const [digits, setDigits] = useState('')
  const [step, setStep] = useState<'phone' | 'code'>('phone')
  const [code, setCode] = useState('')
  const [cooldown, setCooldown] = useState(0)
  const [name, setName] = useState('')
  const [message, setMessage] = useState('')
  const login = useLogin()
  const verify = useVerifyLogin()
  const request = useAccessRequest()
  const phoneRef = useRef<HTMLInputElement | null>(null)
  const caretRef = useRef<number | null>(null)
  const botUrl = import.meta.env.VITE_BOT_URL

  // Контролируемый инпут с маской после ререндера кидает курсор в конец —
  // возвращаем его на позицию, вычисленную в onChange
  useLayoutEffect(() => {
    if (caretRef.current != null && phoneRef.current != null) {
      phoneRef.current.setSelectionRange(caretRef.current, caretRef.current)
      caretRef.current = null
    }
  })

  useEffect(() => {
    if (cooldown <= 0) return
    const t = setInterval(() => setCooldown((c) => c - 1), 1000)
    return () => clearInterval(t)
    // boolean-dep намеренно: интервал перезапускается только на границе 0
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [cooldown > 0])

  const loginCode = login.error instanceof ApiError ? login.error.code : null
  const verifyCode = verify.error instanceof ApiError ? verify.error.code : null
  const requestCode = request.error instanceof ApiError ? request.error.code : null
  const showRequestForm = loginCode === 'UNKNOWN_PHONE'

  const sendCode = () => {
    request.reset()
    verify.reset()
    setName('')
    setMessage('')
    setCode('')
    login.mutate(toApiPhone(digits), {
      onSuccess: () => { setStep('code'); setCooldown(60) },
    })
  }

  if (step === 'code') {
    return (
      <div className="rounded-2xl bg-card p-4">
        <p className="mb-1 text-center text-sm">Код из Telegram</p>
        <p className="mb-3 text-center text-xs text-muted">отправили на {formatPhone(digits)}</p>
        <input
          aria-label="Код из Telegram"
          inputMode="numeric"
          autoFocus
          maxLength={6}
          value={code}
          onChange={(e) => setCode(e.target.value.replace(/\D/g, ''))}
          className="mb-3 w-full rounded-xl border border-muted/40 bg-paper p-2 text-center font-mono text-2xl tracking-[0.4em]"
        />
        <button
          type="button"
          className="w-full rounded-xl bg-ink py-2 text-base text-paper disabled:opacity-50 lg:text-sm"
          disabled={code.length !== 6 || verify.isPending}
          onClick={() => verify.mutate({ phone: toApiPhone(digits), code })}
        >
          Войти
        </button>
        {verifyCode && (
          <p className="mt-2 text-center text-xs text-hanko">
            {VERIFY_ERROR_TEXTS[verifyCode] ?? 'Не получилось — попробуй ещё раз'}
          </p>
        )}
        <div className="mt-3 flex items-center justify-between text-xs">
          <button
            type="button"
            className="text-muted disabled:opacity-50"
            disabled={cooldown > 0 || login.isPending}
            onClick={sendCode}
          >
            Отправить новый{cooldown > 0 ? ` (0:${String(cooldown).padStart(2, '0')})` : ''}
          </button>
          <button type="button" className="text-muted"
            onClick={() => { verify.reset(); setStep('phone') }}>
            Изменить номер
          </button>
        </div>
      </div>
    )
  }

  return (
    <div>
      <div className="rounded-2xl bg-card p-4">
        {/* телефонный инпут — как раньше, класс размера: text-base lg:text-sm */}
        <input
          ref={phoneRef}
          data-testid="phone-input"
          className="w-full rounded-xl border border-muted/40 bg-paper p-2 text-base lg:text-sm"
          placeholder="+7 (___) ___-__-__"
          inputMode="tel"
          value={formatPhone(digits)}
          onChange={(e) => {
            const raw = e.target.value
            const caret = e.target.selectionStart ?? raw.length
            const digitsBeforeCaret = phoneDigits(raw.slice(0, caret)).length
            const next = phoneDigits(raw)
            caretRef.current = caretAfterDigits(
              formatPhone(next),
              Math.min(digitsBeforeCaret, next.length),
            )
            setDigits(next)
          }}
        />
        <button
          type="button"
          className="mt-2 w-full rounded-xl bg-ink py-2 text-base text-paper disabled:opacity-50 lg:text-sm"
          disabled={login.isPending || digits.length !== 10}
          onClick={sendCode}
        >
          Получить код
        </button>
        {loginCode === 'RATE_LIMITED' && (
          <p className="mt-2 text-xs text-hanko">Слишком часто — подожди минуту.</p>
        )}
        {loginCode === 'VALIDATION_ERROR' && (
          <p className="mt-2 text-xs text-hanko">Это не похоже на номер телефона.</p>
        )}
        {loginCode === 'TELEGRAM_NOT_LINKED' && (
          <p className="mt-2 rounded-lg border border-warn-border bg-warn-bg p-2 text-xs text-warn-text">
            Мы шлём код входа в Telegram.{' '}
            {botUrl ? (
              <a href={botUrl} target="_blank" rel="noreferrer" className="underline">Напиши боту</a>
            ) : (
              'Напиши боту'
            )}
            , поделись контактом — и возвращайся.
          </p>
        )}
      </div>
      {/* блок showRequestForm (заявка на доступ) — без изменений, перенести как есть */}
    </div>
  )
}
```

- [ ] **Step 4: Хвосты флоу без ОТП**

- Удалить `OtpModal.tsx` + `OtpModal.test.tsx` (`git rm`).
- `CalendarPage.tsx`:
  - `Flow` → `{ kind: 'idle' } | { kind: 'selecting-reschedule' } | { kind: 'confirm-cancel' } | { kind: 'celebrate'; checkIn: string; checkOut: string }`.
  - Эффект закрытия модалки — закрывать при любом успешном входе (фикс бага «модалка не ушла»):

    ```tsx
    // вход завершился — закрываем модалку; если вход начался с клика по дате,
    // досрочно выбираем её как заезд
    useEffect(() => {
      if (me.data == null) return
      setLoginOpen(false)
      if (pendingDate != null) {
        setSelection({ checkIn: pendingDate, checkOut: null })
        setPendingDate(null)
      }
    }, [me.data, pendingDate])
    ```
  - `submitBooking`: обе ветки в `onSuccess` сразу `setFlow({ kind: 'celebrate', checkIn, checkOut })` (без otp-шага).
  - Диалог `confirm-cancel`: `cancel.mutate(active.id, { onSuccess: () => setFlow({ kind: 'idle' }) })`.
  - Убрать импорт/рендер `OtpModal`, ветку `flow.kind === 'otp'`, пропсы `onEnterCode`/`onCancelPending` у `ProfileCard` (и сами колбэки).
- `ProfileCard.tsx`: удалить блок `b?.status === 'PENDING_OTP'` и пропсы `onEnterCode`/`onCancelPending`.
- `BookingSheet.tsx`: удалить строку `<p ...>код подтверждения придёт в Telegram</p>` и запись `OVERLAPS_OWN_BOOKING` из `ERROR_TEXTS` (код удалён на бэке); инпут комментария: `text-sm` → `text-base lg:text-sm`.
- `TelegramHint.tsx` НЕ трогать: залогинен теперь только привязанный гость, так что плашка фактически не показывается; файл целиком удалит Task 8.
- `MyBookingsPage.tsx`: минимальная правка для компиляции — убрать `useCancelPending`/`OtpModal`/ветку `PENDING_OTP` и `flow 'otp'` (отмена: `cancel.mutate(active.id, { onSuccess: () => { setFlow('idle'); qc.invalidateQueries({ queryKey: ['my-bookings'] }) } })`). Полная перестройка страницы — в Task 8.

- [ ] **Step 5: Прогнать фронтовые тесты и починить флоу-тесты**

Run: `nvm use 22 && npx vitest run && npm run lint && npx tsc -b`
Ожидаемые правки в `App.flow.test.tsx` и тестах страниц: вход теперь = номер → «Получить код» → код `123456` → «Войти» (вынести в хелпер `loginAs()`); бронь после сабмита сразу CONFIRMED (экран «Мы очень вас ждём» без шага кода); упоминания `PENDING_OTP`/«ждёт код»/«Ввести код» удалить.
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A frontend
git commit -m "feat: двухшаговый вход по коду из Telegram, бронь без ОТП"
```

---

### Task 7: Фронтенд — инклюзивная подсветка календаря

**Files:**
- Modify: `frontend/src/lib/selection.ts` + `selection.test.ts`
- Modify: `frontend/src/components/Calendar.tsx` + `Calendar.test.tsx`
- Modify: `frontend/src/pages/CalendarPage.tsx`

**Interfaces:**
- Consumes: `/api/calendar` теперь отдаёт день выезда как BOOKED (Task 4) — «свободен ли день» читается прямо из `days`.
- Produces: `pickDay(selection, dayIso, days)` требует свободным и сам день выезда; `Calendar` подсвечивает диапазон включая `checkOut`; проп `checkoutCandidates` удалён.

- [ ] **Step 1: Failing-тесты**

`selection.test.ts` добавить:

```ts
it('занятый день выезда не даёт закрыть диапазон', () => {
  // 12-е занято: выбрать 10 → 12 нельзя, клик начинает новый выбор
  const days = new Map([[d('2027-07-12'), busyDay('2027-07-12')]]) // хелперы файла
  expect(pickDay({ checkIn: '2027-07-10', checkOut: null }, '2027-07-12', days))
    .toEqual({ checkIn: '2027-07-12', checkOut: null })
})
```

`Calendar.test.tsx` добавить: при `selection = { checkIn: '2027-07-10', checkOut: '2027-07-12' }` ячейки 10, 11 И 12 имеют `data-selected="true"`.

Run: `npx vitest run src/lib/selection.test.ts src/components/Calendar.test.tsx` → FAIL (12-е не выделено).

- [ ] **Step 2: Реализация**

`selection.ts` — проверка занятости включает день выезда:

```ts
// заняты не только ночи [checkIn, dayIso), но и сам день выезда:
// с этапа 6.6 бронь занимает диапазон включительно
const blocked = [...isoRange(checkIn, dayIso), dayIso]
  .some((d) => (days.get(d)?.status ?? 'FREE') !== 'FREE')
```

`Calendar.tsx`:
- `selected`-набор: `isoRange(selection.checkIn, selection.checkOut)` → добавить и `selection.checkOut`:

  ```ts
  const selected = new Set(
    selection.checkIn && selection.checkOut
      ? [...isoRange(selection.checkIn, selection.checkOut), selection.checkOut]
      : selection.checkIn ? [selection.checkIn] : [],
  )
  ```
- Удалить проп `checkoutCandidates` (интерфейс, деструктуризация, `disabled`-логика):

  ```ts
  const disabled = !selectable || iso < today || beyondMax ||
      (status !== 'FREE' && !clickableBusy)
  ```

`CalendarPage.tsx`: удалить вычисление `checkoutCandidates` (IIFE со сканом) и его передачу в оба `<Calendar>`.

- [ ] **Step 3: Прогнать, починить соседние тесты**

Run: `npx vitest run`
`CalendarPage.busy.test.tsx` и `CalendarPage.desktop.test.tsx`: сценарии «клик в чужой день заезда как выезд» удалить — так больше нельзя; моки календаря, где бронь не красит день выезда, дополнить BOOKED-днём выезда (зеркало нового бэкенда).
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add -A frontend
git commit -m "feat: подсветка и выбор дат включают день выезда"
```

---

### Task 8: Фронтенд — навигация и layout (десктоп-панель, мобильные вкладки)

**Files:**
- Create: `frontend/src/components/Greeting.tsx`, `frontend/src/components/TabBar.tsx`, `frontend/src/components/ActiveBookingSection.tsx`, `frontend/src/components/HistoryList.tsx`
- Rewrite: `frontend/src/components/Header.tsx` + `Header.test.tsx`, `frontend/src/pages/MyBookingsPage.tsx` + `MyBookingsPage.test.tsx`
- Modify: `frontend/src/pages/CalendarPage.tsx`, `CalendarPage.desktop.test.tsx`
- Delete: `frontend/src/components/ProfileCard.tsx`, `ProfileCard.test.tsx`, `TelegramHint.tsx`, `TelegramHint.test.tsx`

**Interfaces:**
- Consumes: `useMyBookings`, `useMe`, `useLogout`, `useCancelBooking`, `useUpdateComment` (после Task 6 мутации сами инвалидируют кеши).
- Produces: `Header({ me, onLoginClick? })` — логотип + одна кнопка «выйти»/«Войти», без ссылок навигации; `Greeting({ me })` — телефон + приветствие; `TabBar()` — мобильный сегмент-контрол (роуты `/` и `/my-bookings`), рендерится только под `lg:hidden`; `ActiveBookingSection({ onReschedule })` — карточка CONFIRMED-брони с «Перенести»/«Отменить» (свой confirm-диалог) + комментарий с редактированием; `HistoryList()` — нередактируемый список прошлых поездок.

- [ ] **Step 1: Failing-тесты**

`Header.test.tsx` переписать: залогиненный — есть «выйти», НЕТ ссылок «Календарь»/«Мои брони»; аноним — кнопка «Войти».

`MyBookingsPage.test.tsx` переписать: нет вкладок «Активная»/«История» (нет `aria-pressed`); активная бронь и заголовок «Прошлые поездки» с историей видны одновременно; отмена работает без кода.

`CalendarPage.desktop.test.tsx` дополнить: у залогиненного в правой панели видны приветствие, карточка брони и «Прошлые поездки»; у анонима грид-класса нет (контейнер без `lg:grid`).

Run: `npx vitest run src/components/Header.test.tsx src/pages/MyBookingsPage.test.tsx src/pages/CalendarPage.desktop.test.tsx` → FAIL.

- [ ] **Step 2: Новые компоненты**

`Greeting.tsx`:

```tsx
import type { Me } from '../api/types'

export function Greeting({ me }: { me: Me }) {
  return (
    <div className="mb-3">
      <p className="mb-1 text-xs text-muted">{me.phone}</p>
      <p className="font-display text-base leading-snug">
        {me.greeting ?? `Привет, ${me.name}!`}
      </p>
    </div>
  )
}
```

`TabBar.tsx` (сегмент-контрол, только мобила):

```tsx
import { NavLink } from 'react-router'

const tabClass = ({ isActive }: { isActive: boolean }) =>
  [
    'flex-1 rounded-lg py-1.5 text-center text-sm',
    isActive ? 'bg-paper font-semibold shadow-sm' : 'text-muted',
  ].join(' ')

export function TabBar() {
  return (
    <nav className="mb-3 flex gap-1 rounded-xl bg-card p-1 lg:hidden">
      <NavLink to="/" end className={tabClass}>Календарь</NavLink>
      <NavLink to="/my-bookings" className={tabClass}>Мои брони</NavLink>
    </nav>
  )
}
```

`HistoryList.tsx`:

```tsx
import { useMyBookings } from '../api/queries'
import { isoToRu, nightsWord } from '../lib/dates'

export function HistoryList() {
  const bookings = useMyBookings()
  const history = bookings.data?.history ?? []
  return (
    <div className="mt-4">
      <h3 className="mb-2 font-display text-sm">Прошлые поездки</h3>
      {history.length === 0 && <p className="text-xs text-muted">Пока нет завершённых поездок.</p>}
      <div className="space-y-2">
        {history.map((v, i) => (
          <div key={i} className="rounded-xl bg-card p-2.5 text-sm">
            {isoToRu(v.checkIn)} → {isoToRu(v.checkOut)} · {nightsWord(v.nights)}
          </div>
        ))}
      </div>
    </div>
  )
}
```

`ActiveBookingSection.tsx` — переносится из тела `MyBookingsPage` (карточка CONFIRMED + комментарий + confirm-диалог отмены; статуса PENDING_OTP больше нет):

```tsx
import { useState } from 'react'
import { ApiError } from '../api/client'
import { useCancelBooking, useMyBookings, useUpdateComment } from '../api/queries'
import { isoToRu } from '../lib/dates'

export function ActiveBookingSection({ onReschedule }: { onReschedule: () => void }) {
  const bookings = useMyBookings()
  const update = useUpdateComment()
  const cancel = useCancelBooking()
  const [confirming, setConfirming] = useState(false)
  const [editing, setEditing] = useState(false)
  const [draft, setDraft] = useState('')

  const active = bookings.data?.active ?? null
  if (active == null) {
    return (
      <div className="rounded-2xl bg-card p-3 text-xs text-muted">
        Брони пока нет — выбери даты в календаре.
      </div>
    )
  }

  return (
    <div>
      <div className="rounded-2xl border border-leaf/40 bg-leafbg p-3 text-sm">
        <b>{isoToRu(active.checkIn)} → {isoToRu(active.checkOut)}</b>{' '}
        <span className="rounded-md bg-leaf px-1.5 py-0.5 text-[10px] text-paper">подтверждена</span>
        <div className="mt-2 flex gap-2 text-xs">
          <button type="button" className="flex-1 rounded-lg border border-ink py-1.5"
            onClick={onReschedule}>Перенести</button>
          <button type="button" className="flex-1 rounded-lg border border-hanko py-1.5 text-hanko"
            onClick={() => { cancel.reset(); setConfirming(true) }}>Отменить</button>
        </div>
      </div>

      <div className="mt-3 rounded-2xl bg-card p-3 text-sm">
        {!editing && (
          <div className="flex items-start justify-between gap-2">
            <p className="text-muted">{active.comment ?? 'Без комментария'}</p>
            <button type="button" aria-label="изменить комментарий"
              onClick={() => { setDraft(active.comment ?? ''); setEditing(true) }}>✏️</button>
          </div>
        )}
        {editing && (
          <div>
            <textarea aria-label="Комментарий" maxLength={500}
              className="w-full rounded-xl border border-muted/40 bg-paper p-2 text-base lg:text-sm"
              value={draft} onChange={(e) => setDraft(e.target.value)} />
            <div className="mt-2 flex gap-2 text-xs">
              <button type="button" className="flex-1 rounded-lg border border-ink py-1.5 disabled:opacity-50"
                disabled={update.isPending}
                onClick={() => update.mutate(draft.trim() === '' ? null : draft.trim(),
                  { onSuccess: () => setEditing(false) })}>
                Сохранить
              </button>
              <button type="button" className="flex-1 rounded-lg border border-muted/40 py-1.5"
                onClick={() => setEditing(false)}>Отмена</button>
            </div>
          </div>
        )}
      </div>

      {confirming && (
        <div className="fixed inset-0 z-20 flex items-center justify-center bg-ink/25 p-6">
          <div className="w-full max-w-xs rounded-2xl bg-paper p-4 text-sm shadow-xl">
            <p className="mb-3">Отменить бронь {isoToRu(active.checkIn)} → {isoToRu(active.checkOut)}?</p>
            <div className="flex gap-2 text-xs">
              <button type="button" className="flex-1 rounded-lg border border-ink py-2"
                onClick={() => setConfirming(false)}>Оставить</button>
              <button type="button" className="flex-1 rounded-lg bg-hanko py-2 text-paper disabled:opacity-50"
                disabled={cancel.isPending}
                onClick={() => cancel.mutate(active.id, { onSuccess: () => setConfirming(false) })}>
                Да, отменить
              </button>
            </div>
            {cancel.error instanceof ApiError && (
              <p className="mt-2 text-xs text-hanko">Не получилось — попробуй ещё раз.</p>
            )}
          </div>
        </div>
      )}
    </div>
  )
}
```

(`useCancelBooking` после Task 6 сам инвалидирует me/calendar/my-bookings — отдельный `qc` тут не нужен.)

- [ ] **Step 3: Header, страницы**

`Header.tsx` переписать:

```tsx
import type { Me } from '../api/types'
import { useLogout } from '../api/queries'

export function Header({ me, onLoginClick }: { me: Me | null; onLoginClick?: () => void }) {
  const logout = useLogout()
  return (
    <header className="mb-4 flex items-center justify-between">
      <h1 className="font-display text-2xl">
        Домик в Японии <span className="text-hanko">◉</span>
      </h1>
      {me != null && (
        <button type="button" className="text-sm text-muted" onClick={() => logout.mutate()}>
          выйти
        </button>
      )}
      {me == null && onLoginClick && (
        <button type="button" className="rounded-xl bg-ink px-4 py-1.5 text-sm text-paper"
          onClick={onLoginClick}>
          Войти
        </button>
      )}
    </header>
  )
}
```

`CalendarPage.tsx`:
- Контейнер: грид только для залогиненного —

  ```tsx
  <div className={[
    'mx-auto max-w-md min-h-dvh bg-paper px-4 py-5',
    me.data != null ? 'lg:max-w-[90rem] lg:grid lg:grid-cols-[minmax(0,1fr)_22rem] lg:gap-0 lg:px-10' : 'lg:max-w-[90rem] lg:px-10',
    bothPicked ? 'pb-40' : 'pb-8',
  ].join(' ')}>
  ```
- Правая панель (вместо `ProfileCard`), десктоп-only, с вертикальной линией:

  ```tsx
  {me.data != null && (
    <div className="hidden lg:order-2 lg:block lg:self-stretch lg:border-l lg:border-muted/30 lg:pl-8 lg:ml-8">
      <div className="lg:sticky lg:top-6">
        <Greeting me={me.data} />
        <ActiveBookingSection onReschedule={() => switchFlow({ kind: 'selecting-reschedule' })} />
        <HistoryList />
      </div>
    </div>
  )}
  ```
- Мобильная шапка контента (после `<Header>`): `{me.data != null && (<div className="lg:hidden"><Greeting me={me.data} /><TabBar /></div>)}`.
- Мобильной карточки брони на календаре больше нет (`ProfileCard` не рендерится). Кнопка «Ввести код»/PENDING удалены ещё в Task 6.
- Данные истории тянет `HistoryList` сам (`useMyBookings` с дефолтным enabled; компонент рендерится только для залогиненных, 401-петли нет).

`MyBookingsPage.tsx` переписать:

```tsx
import { Navigate, useNavigate } from 'react-router'
import { useMe } from '../api/queries'
import { ActiveBookingSection } from '../components/ActiveBookingSection'
import { Greeting } from '../components/Greeting'
import { Header } from '../components/Header'
import { HistoryList } from '../components/HistoryList'
import { TabBar } from '../components/TabBar'

export function MyBookingsPage() {
  const me = useMe()
  const navigate = useNavigate()

  // прямой заход анонима — на календарь
  if (me.data == null && !me.isLoading) return <Navigate to="/" replace />
  // на десктопе всё видно на главном экране — отдельной страницы нет
  if (typeof window.matchMedia === 'function'
      && window.matchMedia('(min-width: 1024px)').matches) {
    return <Navigate to="/" replace />
  }

  return (
    <div className="mx-auto max-w-md min-h-dvh bg-paper px-4 py-5 pb-8">
      <Header me={me.data ?? null} />
      {me.data != null && (
        <>
          <Greeting me={me.data} />
          <TabBar />
          <ActiveBookingSection
            onReschedule={() => navigate('/', { state: { startReschedule: true } })}
          />
          <HistoryList />
        </>
      )}
    </div>
  )
}
```

Удалить файлы: `ProfileCard.tsx`, `ProfileCard.test.tsx`, `TelegramHint.tsx`, `TelegramHint.test.tsx` (`git rm`) — залогинен теперь только гость с привязанным Telegram, плашка живёт в `LoginCard`.

- [ ] **Step 4: Прогнать всё, починить хвосты**

Run: `npx vitest run && npm run lint && npx tsc -b`
Ожидаемо: тесты, ссылавшиеся на `ProfileCard`/`TelegramHint`/вкладки, переориентировать на новые компоненты (jsdom: если `window.matchMedia` не замокан в `src/test/setup.ts`, редирект-проверка в тестах MyBookingsPage опирается на его отсутствие — код это учитывает через `typeof`-guard).
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A frontend
git commit -m "feat: десктоп без вкладок с боковой панелью, мобильный сегмент-контрол"
```

---

### Task 9: Фронтенд — мелкие фиксы: курсор, «Передумал», зум iOS

**Files:**
- Modify: `frontend/src/index.css`
- Modify: `frontend/src/pages/CalendarPage.tsx` (баннер переноса)

**Interfaces:**
- Produces: все некликнутые кнопки показывают `cursor: pointer`; «Передумал» — полноценная кнопка; инпуты ≥16px на мобиле (см. классы `text-base lg:text-sm`, расставленные в Tasks 6/8).

- [ ] **Step 1: CSS**

В конец `index.css`:

```css
/* Tailwind v4 preflight ставит кнопкам cursor: default — возвращаем поинтер */
button:not(:disabled) {
  cursor: pointer;
}
```

- [ ] **Step 2: «Передумал» — кнопка**

В `CalendarPage.tsx` баннер переноса:

```tsx
{flow.kind === 'selecting-reschedule' && (
  <div className="mb-2 flex items-center justify-between gap-2 rounded-lg bg-card p-2 text-xs text-muted">
    <span>Перенос: выбери новые даты в календаре.</span>
    <button type="button"
      className="shrink-0 rounded-lg border border-hanko px-3 py-1.5 text-hanko"
      onClick={() => switchFlow({ kind: 'idle' })}>
      Передумал
    </button>
  </div>
)}
```

- [ ] **Step 3: Проверить, что инпуты ≥16px на мобиле**

Run: `grep -rn 'text-xs\|text-sm' frontend/src --include='*.tsx' | grep -E '<input|<textarea|input\b|textarea\b' || grep -rn "className" frontend/src/components/LoginCard.tsx frontend/src/components/BookingSheet.tsx frontend/src/components/ActiveBookingSection.tsx | grep -E "input|textarea"`
Expected: у всех `<input>`/`<textarea>` размер шрифта задан как `text-base lg:text-sm` (16px на мобиле) — поля заявки на доступ в `LoginCard` (`text-xs`) тоже привести к `text-base lg:text-sm`, если ещё не приведены.

- [ ] **Step 4: Прогнать и закоммитить**

Run: `npx vitest run && npm run lint && npx tsc -b` → PASS.

```bash
git add -A frontend
git commit -m "fix: cursor pointer на кнопках, явный «Передумал», без автозума iOS"
```

---

### Task 10: Финальная верификация и учебный разбор

**Files:**
- Create: `docs/learning/08-login-otp-inclusive-ranges.md`
- Modify: `docs/learning/README.md` (строка оглавления)

- [ ] **Step 1: Полные прогоны**

```bash
cd backend-api && ./mvnw test
cd ../bot-service && go test ./...
cd ../frontend && nvm use 22 && npx vitest run && npm run lint && npx tsc -b && npm run build
```

Expected: всё зелёное. Любой красный тест чинится ДО перехода дальше.

- [ ] **Step 2: Живой смоук в Docker**

```bash
docker compose -f docker-compose.dev.yml --profile app up --build -d
```

Прогнать руками на `:3000`: вход по номеру владельца (+77787886432) → код приходит в Telegram → вход; бронь свободных дат → сразу «Мы очень вас ждём», день выезда красится занятым; попытка второй сессии войти без кода — невозможна. Если бот/Telegram недоступен — зафиксировать в отчёте, что смоук отложен, и НЕ считать задачу проваленной.

- [ ] **Step 3: Учебный разбор**

`docs/learning/08-login-otp-inclusive-ranges.md` — тутор-формат с якорями кода (как 00–07, короткий: владелец фронт знает, интересны интеграционные нюансы):
1. Перенос ОТП на вход: почему `issue`/`verifyByAction` переиспользуются без изменений бота (якорь `OtpService.java`, `LoginService.java`).
2. Инклюзивный exclusion constraint: `daterange(..., '[]')`, почему `minusDays(1)` исчез из всех вызовов (якорь `V8__inclusive_booking_range.sql`, `BookingRepository.findOverlapping`).
3. Замена брони одним `@Transactional`-методом: отмена старой + вставка новой, откат при `DataIntegrityViolationException` (якорь `BookingService.create`).
4. UX-мелочи с историей: Tailwind v4 и `cursor: default` у кнопок; автозум iOS при шрифте <16px (якорь `index.css`, `LoginCard.tsx`).

- [ ] **Step 4: Commit**

```bash
git add docs/learning
git commit -m "docs: учебный разбор этапа 6.6 — ОТП на входе и инклюзивные диапазоны"
```
