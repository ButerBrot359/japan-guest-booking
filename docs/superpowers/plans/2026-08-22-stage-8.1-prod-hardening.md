# Этап 8.1 — Прод-hardening бэкенда. План имплементации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрыть security- и чистка-бэклог этапов 2–7: rate-limit через servlet-фильтр с раздельными бакетами и реальным IP за nginx, Secure/TTL куки из конфига, dummy-hash в admin-login, path-guard и constant-time в BotTokenFilter, V9-чистка `PENDING_OTP`, счётчик попыток и чистильщик outbox.

**Architecture:** Пять независимых задач только в `backend-api/`. Лимитер обобщается в `SlidingWindowRateLimiter` (ключ-строка, лимит и окно — параметры) и переезжает из контроллеров в `RateLimitFilter` до всей аутентификации; кука собирается компонентом `AuthCookies`; V9 чистит check-constraint и добавляет `outbox.attempts`.

**Tech Stack:** Spring Boot 4 (Java 21, Maven), Spring Security (servlet-фильтры), Flyway, Testcontainers, Mockito.

**Spec:** `docs/specs/2026-08-22-stage-8.1-prod-hardening-design.md`

## Global Constraints

- Только `backend-api/`; фронт и бот не меняются. Все команды — строго из `backend-api/` (`./mvnw test`).
- Существующие тесты (171) остаются зелёными, кроме осознанных правок, перечисленных в задачах (`BookingRepositoryTest`, `UpdatedAtTriggerTest`, `DatesRaceTest`, `AbstractIntegrationTest`, переименование `LoginRateLimiterTest`).
- Лимиты: оба бакета (`auth`, `requests`) — 5 попыток / 1 минута / IP. Ответ фильтра при превышении: `429`, JSON `{"code":"RATE_LIMITED","message":"Слишком много попыток — подождите минуту"}`.
- Кука: `httpOnly` + `SameSite=Lax` + `path=/` всегда; `Secure` из `app.cookie.secure` (дефолт `false`); TTL = `app.jwt.ttl-days` (хардкод 30 дней удаляется).
- Jackson 3: `ObjectMapper` — из пакета `tools.jackson.databind` (НЕ com.fasterxml).
- Rate-limit остаётся in-memory на один процесс; распределённый — YAGNI.
- Тексты логов/сообщений — по-русски; коммиты `feat:`/`fix:` на русском, стиль репозитория.
- Осознанное отклонение от текста спеки: вместо `void check(key, limit, window)` c исключением — `boolean tryAcquire(key, limit, window)`; спека сама требует удалить `RateLimitExceededException` за ненадобностью (фильтр пишет 429 сам), boolean-контракт это и реализует.

---

### Task 1: `SlidingWindowRateLimiter` + `RateLimitFilter`

**Files:**
- Rename+rewrite: `backend-api/src/main/java/com/batowka/guestbooking/auth/LoginRateLimiter.java` → `SlidingWindowRateLimiter.java`
- Create: `backend-api/src/main/java/com/batowka/guestbooking/auth/RateLimitFilter.java`
- Delete: `backend-api/src/main/java/com/batowka/guestbooking/auth/RateLimitExceededException.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/SecurityConfig.java` (поле + регистрация фильтра, строки ~28-31 и ~51)
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java` (убрать лимитер из login/verify/adminLogin)
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/accessrequest/AccessRequestController.java` (убрать лимитер)
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/common/GlobalExceptionHandler.java` (удалить импорт строки 7 и хендлер строк ~101-105)
- Modify: `backend-api/src/main/resources/application.yml` (добавить `server.forward-headers-strategy`)
- Modify: `backend-api/src/test/java/com/batowka/guestbooking/AbstractIntegrationTest.java` (строки 53, 63 — новый тип лимитера)
- Rename+rewrite test: `backend-api/src/test/java/com/batowka/guestbooking/auth/LoginRateLimiterTest.java` → `SlidingWindowRateLimiterTest.java`
- Create test: `backend-api/src/test/java/com/batowka/guestbooking/auth/RateLimitFilterTest.java`

**Interfaces:**
- Consumes: `Clock`-бин из `SecurityConfig`; `ApiError(String code, String message)` из `common`; `tools.jackson.databind.ObjectMapper`-бин; `BotTokenFilter` (точка регистрации в цепочке).
- Produces: `@Component SlidingWindowRateLimiter` с `boolean tryAcquire(String key, int limit, Duration window)`, `void evictEmpty()` (`@Scheduled`), `void reset()`. `@Component RateLimitFilter` — используется только `SecurityConfig`. Контроллеры больше НЕ знают о лимитере.

- [ ] **Step 1: Переписать юнит-тест лимитера (падающий)**

`git mv` тест и заменить содержимое `SlidingWindowRateLimiterTest.java` (TestClock сохраняется как был):

```java
package com.batowka.guestbooking.auth;

import org.junit.jupiter.api.Test;

import java.time.Clock;
import java.time.Duration;
import java.time.Instant;
import java.time.ZoneOffset;

import static org.assertj.core.api.Assertions.assertThat;

class SlidingWindowRateLimiterTest {

    private static final Duration WINDOW = Duration.ofMinutes(1);

    /** Управляемые часы: тест сам двигает время. */
    private static class TestClock extends Clock {
        private Instant now = Instant.parse("2026-08-19T12:00:00Z");

        void advance(Duration d) { now = now.plus(d); }

        @Override public Instant instant() { return now; }
        @Override public java.time.ZoneId getZone() { return ZoneOffset.UTC; }
        @Override public Clock withZone(java.time.ZoneId zone) { return this; }
    }

    @Test
    void sixthAttemptWithinMinuteIsRejected() {
        var limiter = new SlidingWindowRateLimiter(new TestClock());
        for (int i = 0; i < 5; i++) {
            assertThat(limiter.tryAcquire("auth:1.2.3.4", 5, WINDOW)).isTrue();
        }
        assertThat(limiter.tryAcquire("auth:1.2.3.4", 5, WINDOW)).isFalse();
    }

    @Test
    void otherKeyIsNotAffected() {
        var limiter = new SlidingWindowRateLimiter(new TestClock());
        for (int i = 0; i < 5; i++) {
            limiter.tryAcquire("auth:1.2.3.4", 5, WINDOW);
        }
        assertThat(limiter.tryAcquire("requests:1.2.3.4", 5, WINDOW)).isTrue();
    }

    @Test
    void windowSlidesAfterAMinute() {
        TestClock clock = new TestClock();
        var limiter = new SlidingWindowRateLimiter(clock);
        for (int i = 0; i < 5; i++) {
            limiter.tryAcquire("auth:1.2.3.4", 5, WINDOW);
        }
        clock.advance(Duration.ofSeconds(61));
        assertThat(limiter.tryAcquire("auth:1.2.3.4", 5, WINDOW)).isTrue();
    }

    @Test
    void evictEmptyRemovesIdleKeysAndKeepsActive() {
        TestClock clock = new TestClock();
        var limiter = new SlidingWindowRateLimiter(clock);
        limiter.tryAcquire("old:1", 5, WINDOW);
        clock.advance(Duration.ofMinutes(2));
        limiter.tryAcquire("fresh:2", 5, WINDOW);

        limiter.evictEmpty();

        // старый ключ выселен: с лимитом 1 попытка снова разрешена (счётчик обнулился)
        assertThat(limiter.tryAcquire("old:1", 1, WINDOW)).isTrue();
        // живой ключ счётчик сохранил: лимит 1 уже исчерпан
        assertThat(limiter.tryAcquire("fresh:2", 1, WINDOW)).isFalse();
    }
}
```

- [ ] **Step 2: Убедиться, что тест падает**

Из `backend-api/`: `./mvnw test -Dtest=SlidingWindowRateLimiterTest`
Ожидание: FAIL на компиляции — класса `SlidingWindowRateLimiter` нет.

- [ ] **Step 3: Реализовать `SlidingWindowRateLimiter`**

`git mv LoginRateLimiter.java SlidingWindowRateLimiter.java`, содержимое целиком:

```java
package com.batowka.guestbooking.auth;

import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import java.time.Clock;
import java.time.Duration;
import java.time.Instant;
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

/** Скользящее окно по произвольному ключу (обычно «бакет:IP»). In-memory, на один процесс. */
@Component
public class SlidingWindowRateLimiter {

    private final Map<String, Deque<Instant>> attempts = new ConcurrentHashMap<>();
    private final Clock clock;

    public SlidingWindowRateLimiter(Clock clock) {
        this.clock = clock;
    }

    /** true — попытка разрешена и учтена; false — лимит исчерпан. */
    public boolean tryAcquire(String key, int limit, Duration window) {
        Instant now = clock.instant();
        Deque<Instant> q = attempts.computeIfAbsent(key, k -> new ArrayDeque<>());
        synchronized (q) {
            while (!q.isEmpty() && q.peekFirst().isBefore(now.minus(window))) {
                q.pollFirst();
            }
            if (q.size() >= limit) {
                return false;
            }
            q.addLast(now);
            return true;
        }
    }

    /** Выселяем ключи с опустевшими окнами — иначе мапа растёт на каждый новый IP вечно. */
    @Scheduled(fixedDelay = 600_000)
    public void evictEmpty() {
        // срез по самому длинному используемому окну (1 минута); окна длиннее — увеличить срез
        Instant cutoff = clock.instant().minus(Duration.ofMinutes(1));
        for (Map.Entry<String, Deque<Instant>> e : attempts.entrySet()) {
            Deque<Instant> q = e.getValue();
            synchronized (q) {
                while (!q.isEmpty() && q.peekFirst().isBefore(cutoff)) {
                    q.pollFirst();
                }
                if (q.isEmpty()) {
                    attempts.remove(e.getKey(), q);
                }
            }
        }
    }

    public void reset() {
        attempts.clear();
    }
}
```

- [ ] **Step 4: Прогнать юнит-тест**

Из `backend-api/`: `./mvnw test -Dtest=SlidingWindowRateLimiterTest`
Ожидание: PASS (4 теста). Компиляция всего модуля пока сломана (контроллеры ссылаются на старое имя) — это чинится в следующих шагах, `-Dtest` с юнитом без Spring может тоже споткнуться о компиляцию тестов; в таком случае выполнить Step 5–7 и вернуться.

- [ ] **Step 5: Написать падающий интеграционный тест фильтра**

`backend-api/src/test/java/com/batowka/guestbooking/auth/RateLimitFilterTest.java`:

```java
package com.batowka.guestbooking.auth;

import com.batowka.guestbooking.AbstractIntegrationTest;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.ResultActions;

import static org.springframework.http.MediaType.APPLICATION_JSON;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@AutoConfigureMockMvc
class RateLimitFilterTest extends AbstractIntegrationTest {

    @Autowired MockMvc mvc;

    private ResultActions postLogin(String body) throws Exception {
        return mvc.perform(post("/api/auth/login").contentType(APPLICATION_JSON).content(body));
    }

    @Test
    void sixthLoginAttemptIsRateLimited() throws Exception {
        for (int i = 0; i < 5; i++) {
            postLogin("{\"phone\": \"+79990000001\"}");
        }
        postLogin("{\"phone\": \"+79990000001\"}")
                .andExpect(status().isTooManyRequests())
                .andExpect(jsonPath("$.code").value("RATE_LIMITED"));
    }

    @Test
    void invalidBodyAlsoConsumesAttempt() throws Exception {
        // раньше @Valid срабатывал до rateLimiter.check — мусорные тела не считались
        for (int i = 0; i < 5; i++) {
            postLogin("{}").andExpect(status().isBadRequest());
        }
        postLogin("{}").andExpect(status().isTooManyRequests());
    }

    @Test
    void bucketsAreIndependent() throws Exception {
        for (int i = 0; i < 6; i++) {
            postLogin("{\"phone\": \"+79990000001\"}");
        }
        // бакет auth исчерпан — у формы заявок свой лимит
        mvc.perform(post("/api/access-requests").contentType(APPLICATION_JSON)
                        .content("{\"phone\": \"+79990000002\", \"name\": \"Гость\"}"))
                .andExpect(status().isCreated());
    }

    @Test
    void nonLimitedRequestsAreNotCounted() throws Exception {
        for (int i = 0; i < 10; i++) {
            mvc.perform(get("/api/calendar?from=2026-09-01&to=2026-09-30"));
        }
        // календарь не расходует бакет: логин проходит фильтр (401 = неизвестный номер, не 429)
        postLogin("{\"phone\": \"+79990000001\"}").andExpect(status().isUnauthorized());
    }
}
```

- [ ] **Step 6: Убедиться, что тест фильтра падает**

Из `backend-api/`: `./mvnw test -Dtest=RateLimitFilterTest`
Ожидание: FAIL — компиляция (старые ссылки на `LoginRateLimiter`) либо 6-я попытка не 429.

- [ ] **Step 7: Реализовать фильтр и вычистить контроллеры**

7a. `backend-api/src/main/java/com/batowka/guestbooking/auth/RateLimitFilter.java`:

```java
package com.batowka.guestbooking.auth;

import com.batowka.guestbooking.common.ApiError;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.http.MediaType;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;
import tools.jackson.databind.ObjectMapper;

import java.io.IOException;
import java.time.Duration;
import java.util.Set;

/**
 * Лимитер до всей аутентификации и разбора тела: даже невалидный JSON расходует попытку.
 * Ответ пишем сами — @RestControllerAdvice до servlet-фильтров не дотягивается.
 */
@Component
@RequiredArgsConstructor
public class RateLimitFilter extends OncePerRequestFilter {

    static final int LIMIT = 5;
    static final Duration WINDOW = Duration.ofMinutes(1);

    private static final Set<String> AUTH_PATHS =
            Set.of("/api/auth/login", "/api/auth/verify", "/api/auth/admin-login");
    private static final String REQUESTS_PATH = "/api/access-requests";

    private final SlidingWindowRateLimiter limiter;
    private final ObjectMapper objectMapper;

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        String bucket = bucketFor(request);
        if (bucket == null) {
            chain.doFilter(request, response);
            return;
        }
        // за nginx getRemoteAddr() отдаёт реальный IP клиента (forward-headers-strategy: native)
        if (!limiter.tryAcquire(bucket + ":" + request.getRemoteAddr(), LIMIT, WINDOW)) {
            response.setStatus(429);
            response.setContentType(MediaType.APPLICATION_JSON_VALUE);
            response.setCharacterEncoding("UTF-8");
            objectMapper.writeValue(response.getWriter(),
                    new ApiError("RATE_LIMITED", "Слишком много попыток — подождите минуту"));
            return;
        }
        chain.doFilter(request, response);
    }

    private static String bucketFor(HttpServletRequest request) {
        if (!"POST".equals(request.getMethod())) {
            return null;
        }
        String uri = request.getRequestURI();
        if (AUTH_PATHS.contains(uri)) {
            return "auth";
        }
        if (REQUESTS_PATH.equals(uri)) {
            return "requests";
        }
        return null;
    }
}
```

7b. `SecurityConfig.java`: добавить поле `private final RateLimitFilter rateLimitFilter;` рядом с `botTokenFilter` и строку регистрации после `.addFilterBefore(botTokenFilter, JwtAuthFilter.class)`:

```java
                .addFilterBefore(rateLimitFilter, BotTokenFilter.class);
```

(Итоговый порядок: rateLimitFilter → botTokenFilter → jwtAuthFilter.)

7c. `AuthController.java`: удалить поле `private final LoginRateLimiter rateLimiter;`, три строки `rateLimiter.check(request.getRemoteAddr());` и ставшие ненужными параметры `HttpServletRequest request` у `login/verify/adminLogin` + импорт `jakarta.servlet.http.HttpServletRequest`.

7d. `AccessRequestController.java`: удалить поле `rateLimiter`, строку `rateLimiter.check(...)` с комментарием над ней, параметр `HttpServletRequest request` и импорты `LoginRateLimiter`, `HttpServletRequest`.

7e. Удалить `RateLimitExceededException.java`; в `GlobalExceptionHandler.java` удалить импорт (строка 7) и хендлер `rateLimited` (строки ~101-105).

7f. `AbstractIntegrationTest.java`: заменить оба упоминания `com.batowka.guestbooking.auth.LoginRateLimiter` (строки 53 и 63) на `com.batowka.guestbooking.auth.SlidingWindowRateLimiter` (метод `reset` остаётся).

7g. `application.yml`: добавить на верхнем уровне (рядом с `spring:`):

```yaml
server:
  # за nginx getRemoteAddr() читает X-Forwarded-For; при прямом доступе (dev) ничего не меняет
  forward-headers-strategy: native
```

7h. Проверить, что старое имя нигде не осталось: `grep -rn "LoginRateLimiter\|RateLimitExceededException" src/` → пусто.

- [ ] **Step 8: Прогнать тесты фильтра и лимитера, затем весь модуль**

Из `backend-api/`:
```bash
./mvnw test -Dtest='SlidingWindowRateLimiterTest,RateLimitFilterTest'
./mvnw test
```
Ожидание: PASS оба раза. Если какой-то существующий тест бьёт >5 POST-запросов на лимитируемые пути внутри одного метода — добавить в его начало сброс через autowired `SlidingWindowRateLimiter.reset()` (по образцу `AbstractIntegrationTest`) и отметить это в отчёте.

- [ ] **Step 9: Commit**

```bash
git add -A backend-api
git commit -m "feat: rate-limit переезжает в servlet-фильтр — раздельные бакеты, реальный IP за nginx, учёт невалидных тел"
```

---

### Task 2: `AuthCookies` (Secure + TTL из конфига) и dummy-hash в admin-login

**Files:**
- Create: `backend-api/src/main/java/com/batowka/guestbooking/auth/AuthCookies.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/AuthController.java` (убрать статические `authCookie`/`noContentWithCookie` и `COOKIE_TTL`; dummy-hash в `adminLogin`)
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/common/GlobalExceptionHandler.java` (метод `userGone` — инжект `AuthCookies`)
- Modify: `backend-api/src/main/resources/application.yml` (`app.cookie.secure`)
- Test: `backend-api/src/test/java/com/batowka/guestbooking/auth/AuthCookiesTest.java` (создать)

**Interfaces:**
- Consumes: `JwtAuthFilter.COOKIE_NAME` (существующая константа); проперти `app.jwt.ttl-days` (уже есть, 30) и новая `app.cookie.secure`.
- Produces: `@Component AuthCookies` с методами `ResponseCookie session(String token)`, `ResponseCookie expired()`. Статических методов куки в `AuthController` больше НЕТ — единственный внешний потребитель (`GlobalExceptionHandler.userGone`) переводится здесь же.

- [ ] **Step 1: Написать падающий юнит-тест**

`backend-api/src/test/java/com/batowka/guestbooking/auth/AuthCookiesTest.java`:

```java
package com.batowka.guestbooking.auth;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class AuthCookiesTest {

    @Test
    void secureFlagAppearsWhenEnabled() {
        String cookie = new AuthCookies(true, 30).session("tok").toString();
        assertThat(cookie).contains("Secure").contains("HttpOnly").contains("SameSite=Lax");
    }

    @Test
    void noSecureFlagByDefaultProfile() {
        assertThat(new AuthCookies(false, 30).session("tok").toString()).doesNotContain("Secure");
    }

    @Test
    void ttlComesFromJwtTtlDays() {
        // 30 дней = 2592000 секунд — кука живёт столько же, сколько JWT
        assertThat(new AuthCookies(false, 30).session("tok").toString())
                .contains("Max-Age=2592000");
    }

    @Test
    void expiredCookieErasesValue() {
        String cookie = new AuthCookies(true, 30).expired().toString();
        assertThat(cookie).contains("Max-Age=0").contains("Secure");
    }
}
```

- [ ] **Step 2: Убедиться, что тест падает**

Из `backend-api/`: `./mvnw test -Dtest=AuthCookiesTest`
Ожидание: FAIL — класса `AuthCookies` нет.

- [ ] **Step 3: Реализовать `AuthCookies`**

```java
package com.batowka.guestbooking.auth;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.http.ResponseCookie;
import org.springframework.stereotype.Component;

import java.time.Duration;

/** Сборка auth-куки: httpOnly+SameSite=Lax всегда; Secure и TTL — из конфига. */
@Component
public class AuthCookies {

    private final boolean secure;
    private final Duration ttl;

    public AuthCookies(@Value("${app.cookie.secure:false}") boolean secure,
                       @Value("${app.jwt.ttl-days}") long ttlDays) {
        this.secure = secure;
        this.ttl = Duration.ofDays(ttlDays);
    }

    /** Сессионная кука: живёт столько же, сколько JWT внутри неё. */
    public ResponseCookie session(String token) {
        return build(token, ttl);
    }

    /** Затирающая кука — логаут и «пользователь удалён». */
    public ResponseCookie expired() {
        return build("", Duration.ZERO);
    }

    private ResponseCookie build(String value, Duration maxAge) {
        return ResponseCookie.from(JwtAuthFilter.COOKIE_NAME, value)
                .httpOnly(true)
                .sameSite("Lax")
                .secure(secure)
                .path("/")
                .maxAge(maxAge)
                .build();
    }
}
```

В `application.yml`, в блок `app:` добавить:

```yaml
  cookie:
    # Secure включит прод (HTTPS) через APP_COOKIE_SECURE=true — этап 8.2
    secure: ${APP_COOKIE_SECURE:false}
```

- [ ] **Step 4: Перевести `AuthController` и `GlobalExceptionHandler`**

4a. `AuthController`: удалить `COOKIE_TTL`, статические `authCookie` и `noContentWithCookie`; добавить поле `private final AuthCookies cookies;` и приватный хелпер:

```java
    private ResponseEntity<Void> noContent(ResponseCookie cookie) {
        return ResponseEntity.noContent()
                .header(HttpHeaders.SET_COOKIE, cookie.toString())
                .build();
    }
```

Методы: `verify` → `return noContent(cookies.session(loginService.verify(body.phone(), body.code())));`; `logout` → `return noContent(cookies.expired());`. (Импорт `org.springframework.http.ResponseCookie` добавить, `java.time.Duration` удалить, если не используется.)

4b. Там же — `adminLogin` целиком (dummy-hash):

```java
    /** Хеш строки "password" — публичный BCrypt-тест-вектор. Результат сравнения игнорируется:
        нужен только прогон BCrypt, чтобы время ответа не выдавало, существует ли номер. */
    static final String DUMMY_HASH =
            "$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy";

    @PostMapping("/admin-login")
    public ResponseEntity<Void> adminLogin(@Valid @RequestBody AdminLoginRequest body) {
        UserAccount admin = Phones.normalize(body.phone())
                .flatMap(users::findByPhoneAndDeletedAtIsNull)
                .filter(u -> u.getRole() == Role.ADMIN)
                .filter(u -> u.getPasswordHash() != null)
                .orElse(null);
        // Единый 401 и одинаковое время на любой провал: BCrypt прогоняется всегда
        boolean matches = encoder.matches(body.password(),
                admin != null ? admin.getPasswordHash() : DUMMY_HASH);
        if (admin == null || !matches) {
            throw new InvalidCredentialsException();
        }
        return noContent(cookies.session(jwt.issue(admin.getId(), admin.getRole())));
    }
```

4c. `GlobalExceptionHandler`: добавить `@RequiredArgsConstructor` (импорт lombok) и поле `private final com.batowka.guestbooking.auth.AuthCookies authCookies;`; в `userGone` заменить `com.batowka.guestbooking.auth.AuthController.authCookie("", java.time.Duration.ZERO).toString()` на `authCookies.expired().toString()`.

4d. Проверка хвостов: `grep -rn "authCookie\|noContentWithCookie\|COOKIE_TTL" src/` → упоминаний старых статических методов не осталось.

- [ ] **Step 5: Прогнать точечные и полный сьют**

Из `backend-api/`:
```bash
./mvnw test -Dtest='AuthCookiesTest,*Auth*'
./mvnw test
```
Ожидание: PASS — контракт логина/логаута не изменился (дефолт secure=false, TTL те же 30 дней), существующие auth-тесты зелёные.

- [ ] **Step 6: Commit**

```bash
git add -A backend-api
git commit -m "feat: AuthCookies — Secure и TTL куки из конфига; dummy-hash против тайминг-канала admin-login"
```

---

### Task 3: `BotTokenFilter` — path-guard и constant-time

**Files:**
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/bot/BotTokenFilter.java`
- Test: `backend-api/src/test/java/com/batowka/guestbooking/bot/BotTokenFilterTest.java` (создать)

**Interfaces:**
- Consumes: `AbstractIntegrationTest` уже задаёт `app.bot.api-token=test-bot-token` через `@DynamicPropertySource`; существующий эндпоинт `GET /api/bot/bookings` (этап 6.8).
- Produces: поведение фильтра — `ROLE_BOT` выдаётся ТОЛЬКО на путях `/api/bot/**`.

- [ ] **Step 1: Написать падающий тест**

`backend-api/src/test/java/com/batowka/guestbooking/bot/BotTokenFilterTest.java`:

```java
package com.batowka.guestbooking.bot;

import com.batowka.guestbooking.AbstractIntegrationTest;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@AutoConfigureMockMvc
class BotTokenFilterTest extends AbstractIntegrationTest {

    @Autowired MockMvc mvc;

    @Test
    void validTokenOutsideBotPathsGrantsNothing() throws Exception {
        // токен бота не даёт доступ к админским (и любым не-bot) эндпоинтам
        mvc.perform(get("/api/admin/bookings").header("X-Bot-Token", "test-bot-token"))
                .andExpect(status().isUnauthorized());
    }

    @Test
    void validTokenOnBotPathStillWorks() throws Exception {
        mvc.perform(get("/api/bot/bookings").header("X-Bot-Token", "test-bot-token"))
                .andExpect(status().isOk());
    }
}
```

- [ ] **Step 2: Убедиться, что первый тест падает**

Из `backend-api/`: `./mvnw test -Dtest=BotTokenFilterTest`
Ожидание: `validTokenOutsideBotPathsGrantsNothing` FAIL — сейчас фильтр выдаёт `ROLE_BOT` на любом пути, `/api/admin/**` вернёт 403 (FORBIDDEN, роль есть — прав нет), а не 401. Второй тест зелёный.

- [ ] **Step 3: Реализовать guard + constant-time**

Заменить `doFilterInternal` в `BotTokenFilter.java` (импорты добавить: `java.security.MessageDigest`, `java.nio.charset.StandardCharsets`):

```java
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        // фильтр касается только бот-эндпоинтов — на прочих путях служебный токен ничего не значит
        if (!request.getRequestURI().startsWith("/api/bot/")) {
            chain.doFilter(request, response);
            return;
        }
        String header = request.getHeader("X-Bot-Token");
        // пустой конфиг-токен закрывает эндпоинт; сравнение — constant-time (MessageDigest.isEqual)
        if (!token.isBlank() && header != null && MessageDigest.isEqual(
                token.getBytes(StandardCharsets.UTF_8), header.getBytes(StandardCharsets.UTF_8))) {
            var auth = new UsernamePasswordAuthenticationToken(
                    "bot", null, List.of(new SimpleGrantedAuthority("ROLE_BOT")));
            SecurityContextHolder.getContext().setAuthentication(auth);
        }
        chain.doFilter(request, response);
    }
```

- [ ] **Step 4: Прогнать тесты бота**

Из `backend-api/`: `./mvnw test -Dtest='BotTokenFilterTest,Bot*'`
Ожидание: PASS (новые + существующие BotBookingsTest/BotApprovalTest).

- [ ] **Step 5: Commit**

```bash
git add backend-api/src/main/java/com/batowka/guestbooking/bot/BotTokenFilter.java backend-api/src/test/java/com/batowka/guestbooking/bot/BotTokenFilterTest.java
git commit -m "fix: BotTokenFilter — только /api/bot/** и constant-time сравнение токена"
```

---

### Task 4: Миграция V9 и чистка `PENDING_OTP` из кода

**Files:**
- Create: `backend-api/src/main/resources/db/migration/V9__drop_pending_otp_outbox_attempts.sql`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/booking/BookingStatus.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/admin/BookingsExcelWriter.java` (удалить ветку switch)
- Modify: `backend-api/src/test/java/com/batowka/guestbooking/booking/BookingRepositoryTest.java:42`
- Modify: `backend-api/src/test/java/com/batowka/guestbooking/db/UpdatedAtTriggerTest.java:24`
- Modify: `backend-api/src/test/java/com/batowka/guestbooking/booking/DatesRaceTest.java:62`
- Test: `backend-api/src/test/java/com/batowka/guestbooking/db/SchemaConstraintsTest.java` (добавить тест нового констрейнта)

**Interfaces:**
- Consumes: V8 уже отменил все `PENDING_OTP`-строки и пересобрал GiST-исключение; `one_confirmed_booking_per_user` (V1) уже `WHERE status = 'CONFIRMED'` — V9 трогает только check-constraint.
- Produces: enum `BookingStatus { CONFIRMED, CANCELLED, COMPLETED }`; колонка `outbox.attempts INT NOT NULL DEFAULT 0` — её использует Task 5.

- [ ] **Step 1: Написать падающий тест констрейнта**

В `SchemaConstraintsTest` добавить (jdbc-хелперы вставки пользователей — по образцу существующих тестов этого класса):

```java
    @Test
    void pendingOtpStatusIsRejectedByCheckConstraint() {
        Long userId = jdbcTemplate.queryForObject(
                "insert into users(phone, name) values ('+81999000901', 'Тест') returning id", Long.class);
        assertThatThrownBy(() -> jdbcTemplate.update("""
                insert into bookings(user_id, check_in, check_out, status)
                values (?, '2029-01-10', '2029-01-12', 'PENDING_OTP')
                """, userId))
                .isInstanceOf(DataIntegrityViolationException.class);
    }
```

(Импорты `DataIntegrityViolationException`/`assertThatThrownBy` — если ещё не подключены в классе.)

- [ ] **Step 2: Убедиться, что тест падает**

Из `backend-api/`: `./mvnw test -Dtest=SchemaConstraintsTest`
Ожидание: новый тест FAIL — вставка `PENDING_OTP` пока проходит (V6-констрейнт его разрешает).

- [ ] **Step 3: Миграция V9 + чистка Java**

3a. `V9__drop_pending_otp_outbox_attempts.sql`:

```sql
-- PENDING_OTP мёртв: флоу подтверждения кодом удалён в 6.6, строки отменены в V8. Чистим констрейнт.
ALTER TABLE bookings DROP CONSTRAINT bookings_status_check;
ALTER TABLE bookings ADD CONSTRAINT bookings_status_check
    CHECK (status IN ('CONFIRMED', 'CANCELLED', 'COMPLETED'));

-- Счётчик попыток публикации: ядовитая строка не должна вечно блокировать очередь (этап 8.1).
ALTER TABLE outbox ADD COLUMN attempts INT NOT NULL DEFAULT 0;
```

3b. `BookingStatus.java`: `public enum BookingStatus { CONFIRMED, CANCELLED, COMPLETED }`.

3c. Компиляционные хвосты:
- `BookingsExcelWriter.statusRu`: удалить ветку `case PENDING_OTP -> "не подтверждена";` и комментарий над ней;
- `BookingRepositoryTest.java:42`: `List.of(BookingStatus.PENDING_OTP, BookingStatus.CONFIRMED)` → `List.of(BookingStatus.CONFIRMED)`;
- `UpdatedAtTriggerTest.java:24`: в insert-SQL `'PENDING_OTP'` → `'CANCELLED'` (триггеру всё равно, какой статус);
- `DatesRaceTest.java:62`: `status in ('PENDING_OTP','CONFIRMED')` → `status = 'CONFIRMED'`;
- финальная проверка: `grep -rn "PENDING_OTP" src/` → остаются только комментарии-история (в `SchemaConstraintsTest`, `CalendarServiceTest`, V8-миграции) и строка-литерал в новом тесте Step 1; кода с enum-значением нет.

- [ ] **Step 4: Прогнать затронутые классы и полный сьют**

Из `backend-api/`:
```bash
./mvnw test -Dtest='SchemaConstraintsTest,BookingRepositoryTest,UpdatedAtTriggerTest,DatesRaceTest,BookingsExcelWriterTest'
./mvnw test
```
Ожидание: PASS оба раза.

- [ ] **Step 5: Commit**

```bash
git add -A backend-api
git commit -m "feat: V9 — прощание с PENDING_OTP в схеме и enum, колонка outbox.attempts"
```

---

### Task 5: Outbox — счётчик попыток и чистильщик; лог повышения роли сидером

**Files:**
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/messaging/OutboxPublisher.java`
- Modify: `backend-api/src/main/java/com/batowka/guestbooking/auth/AdminSeeder.java`
- Test: `backend-api/src/test/java/com/batowka/guestbooking/messaging/OutboxTest.java` (дописать 2 интеграционных теста)
- Test: `backend-api/src/test/java/com/batowka/guestbooking/messaging/OutboxPublisherAttemptsTest.java` (создать, юнит с Mockito)

**Interfaces:**
- Consumes: колонка `outbox.attempts` из Task 4 (V9).
- Produces: `OutboxPublisher.MAX_ATTEMPTS = 5` (package-private static final int), публичный метод `cleanupPublished()` — дёргается шедулером и тестом напрямую.

- [ ] **Step 1: Написать падающие тесты**

1a. Юнит `OutboxPublisherAttemptsTest.java`:

```java
package com.batowka.guestbooking.messaging;

import org.junit.jupiter.api.Test;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.kafka.core.KafkaTemplate;

import java.util.List;
import java.util.Map;
import java.util.concurrent.CompletableFuture;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.ArgumentMatchers.contains;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

class OutboxPublisherAttemptsTest {

    @Test
    @SuppressWarnings("unchecked")
    void failedSendIncrementsAttempts() {
        JdbcTemplate jdbc = mock(JdbcTemplate.class);
        KafkaTemplate<String, String> kafka = mock(KafkaTemplate.class);
        when(jdbc.queryForList(anyString())).thenReturn(List.of(
                Map.of("id", 1L, "topic", "t", "payload", "{}")));
        when(kafka.send(any(), any()))
                .thenReturn(CompletableFuture.failedFuture(new RuntimeException("кафка упала")));
        when(jdbc.queryForObject(contains("attempts = attempts + 1"), eq(Integer.class), eq(1L)))
                .thenReturn(1);

        new OutboxPublisher(jdbc, kafka).publishPending();

        // счётчик инкрементнулся, published_at не трогали
        verify(jdbc).queryForObject(contains("attempts = attempts + 1"), eq(Integer.class), eq(1L));
    }
}
```

1b. В `OutboxTest` (интеграционный, стиль существующих; jdbc и publisher — autowired по образцу класса; если `OutboxPublisher` ещё не autowired — добавить `@Autowired OutboxPublisher publisher;`):

```java
    @Test
    void poisonedRowIsSkippedAndQueueMovesOn() {
        jdbcTemplate.update("""
                insert into outbox(topic, event_type, payload, attempts)
                values ('bot-commands', 'POISON', '{"k":"v"}'::jsonb, 5)
                """);
        jdbcTemplate.update("""
                insert into outbox(topic, event_type, payload)
                values ('bot-commands', 'HEALTHY', '{"k":"v"}'::jsonb)
                """);

        // здоровая строка публикуется, ядовитая (attempts=5) не блокирует её и остаётся неопубликованной
        org.awaitility.Awaitility.await().untilAsserted(() ->
                org.assertj.core.api.Assertions.assertThat(jdbcTemplate.queryForObject(
                        "select published_at is not null from outbox where event_type = 'HEALTHY'",
                        Boolean.class)).isTrue());
        org.assertj.core.api.Assertions.assertThat(jdbcTemplate.queryForObject(
                "select published_at is null from outbox where event_type = 'POISON'",
                Boolean.class)).isTrue();
    }

    @Test
    void cleanupDeletesOldPublishedRowsOnly() {
        jdbcTemplate.update("""
                insert into outbox(topic, event_type, payload, published_at)
                values ('bot-commands', 'OLD_PUBLISHED', '{}'::jsonb, now() - interval '8 days')
                """);
        jdbcTemplate.update("""
                insert into outbox(topic, event_type, payload, published_at)
                values ('bot-commands', 'FRESH_PUBLISHED', '{}'::jsonb, now())
                """);
        jdbcTemplate.update("""
                insert into outbox(topic, event_type, payload, created_at)
                values ('bot-commands', 'OLD_UNPUBLISHED', '{}'::jsonb, now() - interval '30 days')
                """);

        publisher.cleanupPublished();

        org.assertj.core.api.Assertions.assertThat(jdbcTemplate.queryForList(
                        "select event_type from outbox where event_type like 'OLD_%' or event_type like 'FRESH_%'",
                        String.class))
                .containsExactlyInAnyOrder("FRESH_PUBLISHED", "OLD_UNPUBLISHED");
    }
```

(Импорты статикой — по стилю файла; `OLD_UNPUBLISHED` затем опубликует фоновый шедулер — тест на состав сразу после `cleanupPublished()`, порядок ассертов важен. Если фоновый publisher успевает опубликовать `OLD_UNPUBLISHED` до ассерта — это не мешает: проверяется только присутствие строки, не её `published_at`.)

- [ ] **Step 2: Убедиться, что тесты падают**

Из `backend-api/`: `./mvnw test -Dtest='OutboxPublisherAttemptsTest,OutboxTest'`
Ожидание: юнит FAIL (нет инкремента `attempts`), `cleanupDeletesOldPublishedRowsOnly` FAIL (нет метода `cleanupPublished` — компиляция).

- [ ] **Step 3: Реализовать**

3a. `OutboxPublisher.java` — заменить тело класса (поля прежние):

```java
    static final int MAX_ATTEMPTS = 5;

    @Scheduled(fixedDelay = 2000)
    public void publishPending() {
        List<Map<String, Object>> rows = jdbc.queryForList("""
                select id, topic, payload::text as payload
                from outbox where published_at is null and attempts < %d
                order by id limit 100
                """.formatted(MAX_ATTEMPTS));
        for (Map<String, Object> row : rows) {
            try {
                kafka.send((String) row.get("topic"), (String) row.get("payload")).join();
            } catch (Exception e) {
                Integer attempts = jdbc.queryForObject(
                        "update outbox set attempts = attempts + 1 where id = ? returning attempts",
                        Integer.class, row.get("id"));
                if (attempts != null && attempts >= MAX_ATTEMPTS) {
                    log.error("Outbox id={} исчерпал {} попыток — строка пропущена, нужен ручной разбор",
                            row.get("id"), MAX_ATTEMPTS, e);
                } else {
                    log.warn("Kafka недоступна, отправка outbox id={} отложена", row.get("id"), e);
                }
                return; // остальные строки подождут следующего цикла — порядок сохраняется
            }
            jdbc.update("update outbox set published_at = now() where id = ?", row.get("id"));
        }
    }

    /** Опубликованные строки старше 7 дней не нужны (в payload бывают сырые OTP-коды). */
    @Scheduled(cron = "0 0 4 * * *", zone = "Asia/Tokyo")
    public void cleanupPublished() {
        int deleted = jdbc.update("delete from outbox where published_at < now() - interval '7 days'");
        if (deleted > 0) {
            log.info("Outbox: удалено {} опубликованных строк старше 7 дней", deleted);
        }
    }
```

3b. `AdminSeeder.java`: добавить `@Slf4j` (импорт `lombok.extern.slf4j.Slf4j`) на класс; в `seed()` после `orElseGet`:

```java
        if (admin.getId() != null && admin.getRole() != Role.ADMIN) {
            log.warn("Сидер повышает существующего пользователя {} до ADMIN", phone);
        }
```

(Поведение не меняется — только лог; существующий `AdminSeederTest` остаётся зелёным, отдельный тест на лог не пишем.)

- [ ] **Step 4: Прогнать точечные, затем полный сьют**

Из `backend-api/`:
```bash
./mvnw test -Dtest='OutboxPublisherAttemptsTest,OutboxTest,AdminSeederTest'
./mvnw test
```
Ожидание: PASS оба раза — финальный полный прогон этапа.

- [ ] **Step 5: Commit**

```bash
git add -A backend-api
git commit -m "feat: outbox — лимит попыток для ядовитых строк и суточный чистильщик; лог повышения роли сидером"
```
